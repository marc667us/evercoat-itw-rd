"""Material Safety Data — the interpretation of safety documentation.

Named in full, never abbreviated. `MSD` in this codebase means the Material
Science & Development Assistant (`app/api/msd.py`, `ai.msd_*`, `msd.use`) and
this is a different thing; the specification writes "Material Safety Data" out
in full all 48 times it uses it, and so does this package.
"""

from __future__ import annotations
