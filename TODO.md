# TODO — EvercoatITWRD APP

**Updated 2026-08-27 (late session). Read `RESUME_HERE.md` first.**

✅ **Closed 2026-08-27 (late): the 19 review findings on `b84a300`/`ad55d99`, and the 11 the repair itself produced.** 🔴 **The worst was mine and it made the live e2e suite run NOTHING while the task reported exit 0** — a duplicate test title in `accessibility.spec.ts` makes Playwright refuse the entire run, and the shell chain that invoked it ended in `tail`, so the reported exit code was `tail`'s. Also mine: a blank `display_name` returned a null profile, which removed `UserMenu`, **which is the only route in the shell to Sign out**; and the Paper theme's alert grounds measured **1.004:1** against its own page, so every notice fill vanished. The theme now covers all 34 colour tokens the source paints with (was 12), `tailwind.config.ts` imports the palette rather than copying it, a pre-paint script kills the light flash, `/api/me` puts `email`/`display_name` back on the MEMBERSHIP where migration 052 had put them, and the accessibility sweep covers 10 pages it had never seen. **Codex 1 finding, Supervisor 10, none overlapping — the 21st consecutive session in which neither alone was enough.**

▶ **Live suite on the deployed demo: 922 passed / 0 failed / 0 skipped** (`api-live` **857**, e2e `shell` **65**), run as two phases. 🔴 The first api-live pass was a **red that was right**: the deployed API was still serving the old `/api/me` shape because the `:18000` listener predated the change. ⚠️ And the e2e run before it was **VOID, not green** — a duplicate test title made Playwright refuse the whole run while the shell chain reported `tail`'s exit code.

⚠️ **NEW: I110** (SECURITY.md §13 states a CSP that does not exist), **I111** (`next build` rewrites `apps/web/tsconfig.json`, swept into three commits now) and **I112** (~~23 DB tests and no route test~~ — **CLOSED**, 13 route tests added and each falsified). All below.

✅ **Closed 2026-08-26 (second session): I106, I108 and I107.** Migration 052 (`k1000`, ADR-031) moves a tenant's view of a member ONTO the membership and makes the GLOBAL identity's `email` and `display_name` unreadable by the runtime roles. 🔴 **The membership columns are not the closure — the revoke is**, because measuring I106 found **I108**: `evercoat_app` held table-level INSERT on `core.organization_members`, so an ORDINARY member (no `admin.users`, no EXECUTE on the bind, no `keycloak_sub`) could manufacture a membership naming any identity, read it, and roll back. **Any membership row turned a global identity into a readable one**; the bind was one of two ways to make one. 046's two trigger guards collapse into ONE partial unique index. **I107** closed with seven end-to-end tests over real HTTP, one of which reproduces the 403-as-500 defect when `_standing_refusal` is broken on purpose.

✅ **Closed 2026-08-26 (third pass): I109 — the sign-in definers answered for ANY subject.** `core.memberships_for_subject(TEXT)` and `core.principal_for_subject(TEXT, UUID)` take a subject as an ARGUMENT and cannot check their caller, because both answer BEFORE a session has an organization. 🔴 **So the fix could not be a check** — a GUC is settable by `evercoat_app` and so is `SET ROLE`; both are misuse barriers. **Privilege had to follow the CONNECTION.** Migration 053 (`l1000`, ADR-032) gives EXECUTE to a new `evercoat_auth` role that holds NO table privilege in any schema and is `NOINHERIT`, reached on a separate pool used by `get_principal` and `/api/me` only. It **fails closed**, and `/health/ready` reports the sign-in connection so a misconfigured deploy is not-ready rather than 403 for every user. ADR-031's pinned-open test was **inverted, not deleted**.

✅ **Closed 2026-08-26: I82** (three migrations — 049 the atomic bind, 050 the standing check, 051 the returned identifier). 🔴 **Each fix introduced the next defect**: 049 granted a cross-tenant WRITE while removing a cross-tenant read; 050 removed `identity_created` and left the same answer inside `user_id`; both left the database's own refusal escaping as a **500**. All three found by review, all three MEASURED before being believed, none found by CI — which was green on every one of them. **Left open on purpose: I106** (the attribute channel) and **I107** (the route has no end-to-end test).

✅ **Closed today: I100** (the live suite refuses to report green over coverage it lacks), **I83** (the cross-tenant email oracle), **I81** (an authentication identifier is not a readable column), **I102** (the suite locked itself out of Keycloak and blamed the password). ✅ **And §0.2's conductor tier is complete** — laboratory, testing, MSD and a new **analysis** department, each owning its permission gate.

🔴 **I100 CLOSED — the live suite no longer reports green over coverage it does not have.** `scripts/live-suite.sh` now runs a PREFLIGHT that names every capability, the variables it needs and the tests it governs, and exits 2 rather than print three numbers it cannot back. Half-configured is always a hard failure; absent needs an explicit `--allow-partial`, which names every gap in the report and cannot waive a database the run can already reach. Falsified in eleven directions, not asserted.

▶ **Live suite on the deployed demo, after migration 052: 839 passed / 0 failed / 0 skipped** (`api-live` 802, e2e `shell` 37). Preflight reported all four capabilities CONFIGURED -- no `--allow-partial` -- and `tmp/live-suite/e2e.json` records both `sign-in.spec.ts` tests as `['passed']`, i.e. they RAN rather than merely not being skipped (I100). ⚠️ **The demo API had to be restarted first**: 052 was applied to the database the running demo uses, so it was briefly old code against a new schema -- and `/health/ready` stayed 200 the whole time, because a health check does not read the revoked columns.

▶ **All four target modules are wired.** 23 of 37 endpoints across Laboratory, Testing, Formulations and MSD had **no browser caller**; all 37 do now (Laboratory 10→11, Testing **1→9**, Formulations **1→13**, MSD 2→4). New workspaces at `/testing/test?id=…` and `/formulations/formula?version=…`.

🔴 **713 GREEN AND BROWSER SIGN-IN WAS 404 (I96).** Caddy's `/auth/*` identity prefix swallowed the app's own OIDC callback. Nothing in the suite traversed it — `api-live` uses direct grant, the e2e shell suite uses a seam compiled out of production builds. **I97 closed it** with a real sign-in test, proven to fail in both broken states. *Verify the path a human uses, not the one that is easy to script.*

▶ **The demo stack now OUTLIVES the session.** `powershell -File scripts\demo-up.ps1` starts cloudflared + API + web **detached**, derives the tunnel hostname from cloudflared's own log, repoints all four things that carry it, and **verifies the Keycloak client by read-back**. Containers carry `--restart unless-stopped`. Full notes: `Documents/session-archives/2026-08-24/RESTART_THE_DEMO.md`.

⏳ **The API + Keycloak Render deploy still waits for 2026-09-01** — see D1 below. Nothing about the local/tunnel demo depends on it.

---

## ▶ NEXT SESSION — do these in this order

**Where the build stands:** MVP-1 Slices 1–5 complete, **Slice 6 complete as of
this session** (browser surface built, both reviewers passed), Slice 7
**partially complete**, Slice 8 built ahead. 8 of 32 phases done.

| # | Task | Why now |
|---|---|---|
| 1 | 🔴 **Finish Slice 7 — the current phase.** Messaging has **0 of 5 endpoints pressable**, and there is no notifications screen (**I12**). | Slice 7 is the MVP-1 release gate and it is the only MVP-1 slice with unbuilt browser surface left. The same audit that found 31-of-79 has never been applied to messaging: the endpoints, the permissions and the tables all exist. **Ask of each: which production path reaches it?** |
| 2 | **I110 — `SECURITY.md` §13 states a CSP that does not exist.** | Measured this session, twice, independently: no `Content-Security-Policy` in `infrastructure/compose/Caddyfile`, `next.config.mjs`, `render.yaml` or the live response. The document asserts a control nothing implements — this project's most-repeated defect class, in the security document. **Two things are owed and they are separate:** correct the claim, and decide whether to add the header. If it is added, the pre-paint script in `app/layout.tsx` needs its `sha256-` hash in `script-src` or the dark theme silently stops applying — nothing fails, the colour is just wrong. Codex raised it; the fix is a deployment decision, not a code change. |
| 3 | **I76 / I77** — MSD relevance is still lexical | A neural embedder is the real fix. 🔴 `MAX_DISTANCE = 0.74` is calibrated on six queries and **must be re-derived, not inherited**. |
| 4 | 🔴 **I56 / I58** — FORCE RLS cutover | `core.users` and `core.user_id_for_subject` are in scope, and 046/047 add two SECURITY INVOKER trigger functions that must still see their own tenant's rows under FORCE. 🔴 **Carries an owed measurement:** ADR-030's `core.authorization_for_current_session()` reads six `core` tables as `evercoat_owner` and its behaviour under FORCE is **UNMEASURED** — the test was written and withdrawn because `ALTER TABLE … FORCE` needs ACCESS EXCLUSIVE and hangs against a live pool. Measure it during the cutover, when those tables are being altered anyway. |
| 5 | **I78** — the knowledge document list truncates at 100, silently | `list_documents(limit=100)` and no caller passes it. |
| 6 | **I111 — `next build` rewrites `apps/web/tsconfig.json` on every run** | Third time it has been swept into a commit; the third one carried `.next-review2/types`, a path from the Supervisor's verification build that exists on nobody's machine. Reverted each time by hand, which is a reflex rather than a fix. `include` carries real entries so gitignoring the file would take those with it — **this is a decision**, not a cleanup: a committed `include` that the build is allowed to reorder, or a build flag that stops it. |
| 7 | **I101** — 595 orphaned test users in the development database | `test_023_messaging`'s fixture leaked one identity per run for months (fixed 2026-08-25; the debris remains). **Any measurement over `core.users` is mostly measuring test garbage.** Deleting them is a judgement call, not a defect fix. |
| 8 | **`TODO.md` D1** — deploy API + Keycloak | **On/after 2026-09-01**, when Render frees the one free-tier database slot. 🔴 Do NOT delete `autoworkshop-postgres` early — its app data is unarchived. Needs `AUTH_DATABASE_URL` and its `ALTER ROLE`. |
| 9 | **Then Slice 9 — the next phase.** Advanced test analysis: confidence intervals, ANOVA, control limits, outlier rules, test analytics. | The natural successor now that Slice 6 is closed: it deepens the module Slice 5 built and feeds Slice 10's failure intelligence, which **depends on Slice 6** and is therefore unblocked for the first time. |
| — | *— below this line: closed, kept for the lesson —* | |
| 6 | **I109** | ✅ **CLOSED 2026-08-26** — migration 053 (`l1000`), ADR-032. Kept as a pointer for the lesson: two candidate fixes were rejected as decoration because anything able to run SQL as `evercoat_app` can set a GUC or assume a role, so the boundary had to be the CONNECTION. Codex's review of the fix produced seven findings — one measured WRONG, six fixed — and the Supervisor found a seventh. |
| 7 | **I106 / I107 / I108** | ✅ **CLOSED 2026-08-26** — migration 052 (`k1000`), ADR-031, and `tests/auth/test_admin_member_routes.py`. Kept as a pointer: the closure is the REVOKE on `core.users`, not the membership columns, and the reframing came from measuring the first defect rather than from fixing it. |
| 8 | **I82** | ✅ **CLOSED 2026-08-26** — 049/050/051. Kept here only as a pointer: the closure took THREE migrations because each fix introduced the next defect (cross-tenant write → oracle moved into `user_id` → refusal escaping as a 500). `user_id_for_subject` hands out a uuid for an arbitrary subject — **the oracle shape ADR-030 deliberately avoided** by taking zero arguments. 🔴 Its original fix is REJECTED on evidence (ADR-029). ⚠️ **But re-measure the rejection before designing around it:** ADR-029's mechanism was a definer's WRITE firing ADR-028's guards as owner, and 047 then made BOTH guards scope-explicit. That may have removed the reason. Measure it; do not assume either way. |

**Standing obligation each session:** run the full live suite after any deploy and report **passed / failed / skipped as three numbers**. 🔴 **The script now enforces this itself** — it refuses to run without the environment its coverage depends on, and the sign-in round trip is no longer something to remember: a skip there is counted as a FAILURE. See `CLAUDE.md` §13 for the incantation.

---

## 0. Blocking — do these before anything else

### ⏳ D1 — DEPLOY THE API. Waits for 2026-09-01, then it is ~20 minutes.

**Owner decision, 2026-08-22: stay at $0 and wait.** Everything that does not
depend on the wait is already done.

**Why it waits.** Render answered the provisioning attempt exactly:

```
POST /postgres  plan=free  ->  400
{"message": "cannot have more than one active free tier database"}
```

The one free-tier slot is held by **`autoworkshop-postgres`**, which Render
**deletes on 2026-09-01**. The API cannot start without a database:
`database_url` is `Field(...)` in `app/core/config.py`, not optional.

🔴 **DO NOT free the slot early by deleting `autoworkshop-postgres`.**
AutoWorkshop's 8 Keycloak users are archived and verified; **its APP DATA IS
NOT**, and the retirement is flagged BLOCKED. It expires on its own in days.

**On or after 2026-09-01, in order:**

1. `gh workflow run render-provision.yml -f resource=postgres -f confirm=CREATE`
   (plan defaults to `free`). Confirm a 201, not another 400.
2. `gh workflow run render-provision.yml -f resource=api-service -f plan=free -f confirm=CREATE`
   — docker env, `rootDir: apps/api`, health check `/health/live`,
   `autoDeploy: no`.

   ⚠️ **THIS STEP HAS ITS OWN QUOTA AND IT IS A DIFFERENT ONE.** Measured
   2026-08-21: `POST /services plan=free` → **400 "free tier usage quota has
   been exhausted"** — that is the 750 free INSTANCE-HOURS/month, separate from
   the one-free-database limit blocking step 1. It resets with the billing
   cycle and has **not** been re-tested since (re-testing it means actually
   creating a service, which is pointless before the database exists). If it
   400s again, the database from step 1 is harmless and costs nothing; stop and
   report rather than reaching for a paid plan.
3. Set `DATABASE_URL` (the new database's internal URL), **`AUTH_DATABASE_URL`**
   and `KEYCLOAK_ISSUER` on the service, then deploy it.

   🔴 **`AUTH_DATABASE_URL` IS NEW AND THE DEPLOY AUTHENTICATES NOBODY WITHOUT
   IT** (I109, migration 053). `evercoat_app` no longer holds EXECUTE on
   `core.principal_for_subject` or `core.memberships_for_subject`; the
   `evercoat_auth` role does, and migration 053 creates it **NOLOGIN**, because
   a migration must not carry a password. So step 4 is not finished until:

   ```sql
   ALTER ROLE evercoat_app  LOGIN PASSWORD '<app password>';
   ALTER ROLE evercoat_auth LOGIN PASSWORD '<auth password>';
   ```

   ⚠️ **Do not point it at the same role as `DATABASE_URL`** — the whole
   mechanism is that privilege follows the connection. `/health/ready` refuses
   that, and refuses a superuser, and `app/core/config.py` refuses a superuser
   at startup. Confirm `/health/ready` reports `"sign_in":"ok"` BEFORE running
   the live suite: a 503 there means the suite would measure an API nobody can
   log in to.
4. Run migrations against the new database — `alembic upgrade head`, currently
   head `e7000`.
5. **Run the live suite with the `full` profile**, not `web`:
   `./scripts/live-suite.sh <api-url> full`. Report **three numbers**. The two
   skips that profile `web` reports today are precisely the API coverage this
   step is meant to convert into real results.

**✅ The artifact is ready and PROVEN.** `apps/api/Dockerfile` had **never
built** — the runtime stage's `--no-index` editable install could not reach
`hatchling`, then `editables`. Fixed 2026-08-22 and verified by running the
image against the real database: `/health/live` 200, `/health/ready`
`{"database":"ok","migrations":"ok"}`, `/api/knowledge/documents` 401
anonymous, `/docs` 200. CI job **`api-image`** now builds it on every run, so
this cannot rot again before 09-01.

### 🔑 KEYCLOAK — AND IT FITS ON THE FREE TIER AFTER ALL

**Owner asked for Keycloak too, 2026-08-22.** It goes in with the API on
09-01, as a 4th step, and it should cost **nothing**.

🔴 **THE "KEYCLOAK CANNOT RUN ON RENDER FREE" CONCLUSION WAS WRONG — IT WAS
MEASURED WITH AN UNTUNED JVM.** The record said 451.6/512 MiB, OOM-killed
(exit 137), and that Render has nothing between Starter $7 and Standard $25.
Re-measured on 2026-08-22 with a hard `--memory=512m` and a bounded heap:

| mode | result | steady state |
|---|---|---|
| `start-dev --import-realm` | started, realm imported, discovery 200 | **407 MiB / 512** |
| `start` (**production**) | started, discovery 200, token endpoint rejects a bad secret correctly | **387 MiB / 512** |

The setting that makes the difference:

```
JAVA_OPTS_KC_HEAP="-Xms48m -Xmx256m -XX:MaxMetaspaceSize=128m"
```

Unconstrained, the JVM sizes its heap against the HOST — the running demo
container measures **676.6 MiB** on a 3.78 GiB machine. Given a 512 MiB
container and no `-Xmx`, it sizes badly and dies. **The old measurement was of
an untuned JVM, not of Keycloak.** Neither number was wrong; the conclusion
drawn from it was.

⚠️ **COLD START IS ~190 SECONDS IN PRODUCTION MODE, AND FREE SERVICES SPIN
DOWN.** For an identity provider that is a real problem: the first sign-in
after an idle period will very likely time out before Keycloak answers. It is
a usability constraint, NOT an impossibility, and it is the thing most likely
to make somebody say "auth is broken" when it is merely asleep. Decide with
open eyes; Starter removes the spin-down.

⚠️ **~125 MiB of headroom is thin.** It is enough at rest with one realm and
no users. It has NOT been load-tested, and the realm currently has **0 users**
(`services/keycloak/realm/evercoat-realm.json` — 3 clients, 10 realm roles, no
users), so users must be created before anyone can sign in.

**Step 4, on 09-01 after the API is up:**

4a. Create a `keycloak` **schema** in the same `evercoat-itw-rd-postgres` —
    Keycloak takes `KC_DB_SCHEMA`, so it needs **no second database** and does
    not consume another free-tier slot. This is how AutoWorkshop does it.
4b. Create the Keycloak web service on **free**, image
    `quay.io/keycloak/keycloak:26.0`, command `start`, with
    `JAVA_OPTS_KC_HEAP` exactly as above, `KC_DB=postgres`, `KC_DB_SCHEMA=keycloak`,
    `KC_HTTP_ENABLED=true`, `KC_PROXY_HEADERS=xforwarded`, and `KC_HOSTNAME`
    set to its public URL.
4c. Import `services/keycloak/realm/evercoat-realm.json`, then **create users** —
    the realm ships with none.
4d. Set `KEYCLOAK_ISSUER` on the API to `<kc-url>/realms/evercoat` and redeploy it.

4e. Run `scripts/keycloak-bootstrap.sh`. **The realm has ZERO users** and this
    is what creates them — ten, one per realm role, matching `scripts/seed.py`
    exactly: `chem.demo`, `eng.demo`, `lead.demo`, `dir.demo`, `qa.demo`,
    `tech.demo`, `proc.demo`, `prod.demo`, `exec.demo`, `admin.demo`. It needs
    `KC_ADMIN_PASSWORD` and `KC_USER_PASSWORD`.

**🔑 THE CREDENTIALS ALREADY EXIST — created 2026-08-23, in GitHub Secrets on
this repo, which is the authoritative store on this platform:**

| Secret | Used as | For |
|---|---|---|
| `KC_BOOTSTRAP_ADMIN_PASSWORD` | Keycloak's `KC_BOOTSTRAP_ADMIN_PASSWORD`, and `KC_ADMIN_PASSWORD` for the bootstrap script | the `admin` master account |
| `KC_USER_PASSWORD` | `KC_USER_PASSWORD` for the bootstrap script | all ten `*.demo` users |

Set with `printf '%s' | gh secret set` — **never** through a PowerShell pipe,
which appends a BOM to the value ([[feedback_powershell_pipe_bom_secrets]]).
The owner holds copies for testing. **Neither has ever been used**, because
the service does not exist yet; rotate them if that changes.

---

**THE SHARED-INSTANCE ROUTE WAS TRIED ON 2026-08-23 AND IS CLOSED.**

The owner approved importing the `evercoat` realm into the existing paid
`solarpro-keycloak` (`auth.aiappinvent.com`) to avoid the cold start. It could
not be done: **the master admin password has been rotated** since the value in
`project_solar_pv_credentials` (`marble-willow-poppy-river`, 2026-06-13), which
now returns `Invalid user credentials`, and **the owner does not hold the
current one**. GitHub Secrets are write-only, so it cannot be read back.

It was NOT worked around. Fetching it through a workflow would have printed a
live production password into a build log — `KC_BOOTSTRAP_ADMIN_PASSWORD` is
not a registered secret on THIS repo, so GitHub would not mask it.

`autoworkshop-keycloak` is separately ruled out: its database is
`autoworkshop-postgres`, which Render **deletes 2026-09-01**, so a realm
imported there would work for days and then vanish.

Solar was left untouched and verified after the attempt: master realm 200,
`solarpro` realm discovery 200, `solarpro.aiappinvent.com` 200.

**Until step 4 lands, do not describe the API as "deployed and working".**
After steps 1–3 it serves `/health`, `/docs` and correct 401s and **no
authenticated route**, which means the RAG is not usable end to end.


| # | Task | Why it blocks |
|---|---|---|
| ~~B1~~ | ~~Read the `bash -x` trace in the Auth job.~~ | ✅ **CLOSED 2026-08-19.** A literal `\n` became a bare word `n`, which curl read as a second URL — exit 6, status `204000`. Four more defects sat behind it. **Auth is now green: passed=7 failed=0 skipped=0.** |
| ~~B0~~ | ~~Decide S1's auth architecture.~~ | ✅ **CLOSED 2026-08-19 — ADR-025.** Browser-side OIDC Authorization Code + PKCE against the existing public `evercoat-web` client. next-auth removed: it needs server route handlers and this deploys as a static export. |
| ⚠️ **B4** | **Render's push webhook is not firing. A manual lever now exists.** | `render-audit` reports the service correct — `autoDeploy: yes`, `branch: master`, right repo, not suspended — and it had not deployed since **2026-08-18T20:09:24Z**. Every commit for a day and a half was un-deployed while CI went green. **Fixed for now by `.github/workflows/deploy-web.yml`** (`gh workflow run "Deploy web (manual)"`), which resolves the service BY NAME, refuses unless it finds exactly one `evercoat-itw-rd-web`, issues one POST, waits for a terminal state, and then proves the edge changed. Ran it: the site now serves `last-modified: 2026-08-20 00:50:31 UTC` and `/auth/callback/` answers **200**. 🔴 **The ROOT CAUSE is still open** — the webhook. That is a dashboard action (reconnect the GitHub integration) and therefore the operator's. Until then every deploy must be triggered by hand. **Never** use `render-setup.yml` apply mode to force one: it DELETEs AutoWorkshop custom domains. |
| 🔴 **B5** | **The live site shows "Not signed in" and NO Sign in button — by build configuration, not by defect.** | Three causes stacked, and only the first is now fixed. (1) ~~the site was serving an 08-18 build~~ — fixed, see B4. (2) **`render.yaml` sets no `NEXT_PUBLIC_KEYCLOAK_URL`**, and `NEXT_PUBLIC_*` is inlined at BUILD time — so the deployed bundle has no identity provider compiled in and correctly renders "Not signed in" with the reason. (3) **There is no Keycloak deployed to point it at** (I13). Setting the variable without (3) would compile in an address that answers nothing, which is worse than an absent one — the file says so already. So the button appears when a Keycloak exists AND the variable is set AND the site is rebuilt. Deploying Keycloak needs a Render web service — **spend, and the operator's decision.** |
| **B2** | **Establish whether the out-of-state client can reach the site *now*, and which URL they were given.** | Every server-side check passed again 2026-08-19: live suite **25/0/2** against the deployed URL; root, `/dashboard/` and `/admin/` all 200. `www.` fails — no record. Do not change the deployment before proving it is broken for them. |
| **B3** | **The demonstration-data banner is on every live screen.** | Confirmed by measurement — visible text on `/dashboard`, `/projects`, `/formulations`, `/materials`, `/my-work`. By design today (I2) and the operator has flagged it. It clears when S2 wires the screens, which is blocked behind S1, which is blocked behind B0. |


---

## 1. Issues — open defects and gaps, ranked

### 🔴 P1 — gates the MVP acceptance criterion

| # | Issue | Detail |
|---|---|---|
| ~~I1~~ | ~~No sign-in flow.~~ | ✅ **BUILT 2026-08-19 (ADR-025).** Browser-side OIDC + PKCE: `lib/auth/*`, `AuthProvider`, `/auth/callback/`, `AccountMenu` with a real organization switcher. `GET /api/me` + migration 024 close the circularity that made a valid token useless. Auth job **11/0/0** against a real Keycloak, including "the organization id from /api/me is accepted by a real route". **Not yet exercisable by a human: no Keycloak is deployed (I13), and see B4 — the site has not deployed at all since 08-18.** |
| ~~I2~~ | ~~11 of 12 web screens render `demo-data.json`.~~ | ✅ **S2, 2026-08-19.** Five list screens now issue real requests: **Projects, Formulations, My Work, Suppliers, Materials**. The fixture is still IMPORTED by each of them, by design — it is the labelled fallback when there is no API address compiled in or no session. Remaining on the fixture only: Dashboard, Innovation, Pipeline, and the two detail screens. |
| 🟡 **I3** | **The golden Playwright E2E does not exist.** — *the DATABASE half now does.* | ✅ **HALF DONE 2026-08-21** (`66c3921`): `apps/api/tests/db/test_golden_scenario.py` walks **all fifteen arrows** through the real services against a real PostgreSQL — the first time this project's digital thread has ever run whole. Asserts the project→opportunity link, that a batch cannot come from an unapproved formula, RED + automatic investigation (I6), the revision NAMING its failure (I7), genealogy, 🔴 **YELLOW while approvals are outstanding**, GREEN only after review+approval, and finally WALKS THE THREAD BACKWARDS in one query from the approved result to the opportunity. 🔴 **Writing it found the FIRST arrow had no production path**: an opportunity was created in `draft` and nothing could move it to a decidable status, so it could never become a project. Fixed (`submit_opportunity` + route). ⚠️ **STILL OPEN**, and this is the gate: §44 requires the scenario **on the DEPLOYED instance** with every arrow asserted in **UI and** database state. The UI half needs screens that do not exist for most steps (I4 dashboards); "deployed" needs I13. The RBAC/MSD-boundary suite is also still unwritten. |
| 🟡 **I23** | **TEST-RESULT EXPLANATION BUILT 2026-08-22** (§17). `app/agents/tools/testing.py` + the `explain_result` intent. 🔴 **It computes NOTHING** — §10's disposition comes from an ordered fourteen-rule algorithm that `get_test` runs on every read, and a chat capability that recomputed a mean or re-decided a colour would be **a second implementation of the safety algorithm reachable from a chat box**. A test asserts the explanation equals what the engine derived, field for field. It states the automatic evaluation and the final disposition **separately** (§10), names the **rule number** that decided the colour so "why" is checkable rather than plausible, and carries the raw replicates because an aggregate alone is not the record. ⚠️ **Ordering decision:** a question naming a test number is checked BEFORE guidance — *"why did T-0001 fail?"* is about T-0001, and answering it with a general explanation of RED reads as an answer while being none; *"why did the test fail"* with no identifier still goes to guidance. ▶ **STILL OPEN: knowledge/RAG search** — Slice 8, needs pgvector (ADR-013). MSD is now **7 of 9** capabilities | P2 |
| **I23-was** | **MSD's remaining Concept-Note capabilities.** | Built 2026-08-20: application guidance, pending work, record search, **material safety/SDS (§11, all four named questions)** and **the formulation equations (§8/§17, delegating to `evaluate_version`)**. NOT yet built, each with an engine already waiting for it: **formula comparison (§9)** — `compare_versions` exists; **test-result explanation (§17)** — `replicate_statistics` and `derive_disposition` exist; **knowledge/RAG search** — Slice 8 per ADR-013, needs pgvector. Phases 3–6 of Concept Note §39 (formulation recommendations, DOE interpretation, predictive what-if) are beyond MVP-1 by design. |
| ~~I22~~ | ~~MSD has no HTTP route at all.~~ | ✅ **BUILT 2026-08-20.** Root Orchestrator → MSD Conductor → six tools, four routes under `/api/msd`, and a side panel replacing the disabled top-bar placeholder. Migration 026 closed a conversation leak found while building it. |
| ~~I4~~ | ~~No dashboards.~~ | ✅ **CLOSED 2026-08-21** (`b862d3d`, `b6826aa`). Chemist, Engineer, Lead and Director at `GET /api/dashboards/{role}`, with the panels the source names for each (`ITWRD App.txt` L7187-7261). Record panels carry their source id (§2 drill-down); FACET panels (`pipeline_status`, `rd_portfolio`, `innovation_pipeline`) are grouped counts and are named as such rather than pretending otherwise. Panels needing Slices 12-18 return `available: false` **with the slice that will build them** — not omitted, because an omitted panel reads as "nothing to report". 🔴 **The failure mode here is an EMPTY panel, not an error** — a filter on a value the vocabulary lacks returns zero rows and looks healthy. Three were in the first draft (`risks.impact='critical'` when it is low|medium|high; `milestones.title`/`due_date`; `status <> 'complete'`), caught by checking the live schema. ⚠️ **Codex then found 10 more**, incl. an approvals panel that **checked no permission at all** and `blocked_projects` **counting blockers rather than projects** (5 blockers on 1 project reported as 5). The approvals panel now calls the engine's own `pending_steps_for` rather than re-implementing reachability — a rule that changed twice in one session. |

### 🟠 P2 — real defects in shipped code

| # | Issue | Detail |
|---|---|---|
| 🟠 **I78** | **The knowledge document list truncates at 100, silently.** | `list_documents(limit=100)`; `GET /api/knowledge/documents` never passes it and the screen shows no truncation notice, so at 101 documents the oldest simply stop appearing — no page two, no count. That is the same unanswerable *"why is my document not here?"* the `chunks` column was added to prevent, which is why it is written down rather than left. Needs pagination, or at minimum a "showing the most recent 100" line. Found by the Supervisor. |
| ~~I79~~ | **The web client does not know the caller's own permissions.** | `GET /api/me` returns `organizations[].roles` and **no permissions**, and `app/layout.tsx` hands the sidebar `ALL_NAV_PERMISSIONS` — the full module map — so every permission-shaped decision in the UI is currently made against what the app *could* offer rather than what *this user* holds. Surfaced by the knowledge ingest form: it cannot be hidden from a Chemist who lacks `knowledge.ingest`, so it is shown to everyone and the server's 403 is translated into a plain sentence instead. §6 says frontend checks are cosmetic and the server re-enforces — which it does — so this is a UX and honesty gap, not a security hole. Fix: add `permissions` to `/api/me` and thread it through the layout. | ✅ **CLOSED 2026-08-25** (`88dbc5a`, `6eb6323`) — migration **045 / e9000**. `core.memberships_for_subject` now returns permission codes per organization, resolved through the same `member_roles → roles → role_permissions → permissions` chain `core.principal_for_subject` (033) already walks, so there is ONE definition rather than a TypeScript copy that cannot be type-checked against it. Measured: lead.demo **38**, tech.demo **11**. 🔴 **A DROP TAKES THE OWNER, THE GRANT *AND* THE COMMENT** — all restated in both directions, and the migration is proven **e9000 → e8000 → e9000** with shape, owner, ACL and comment asserted at each end; the downgrade had never been run before. Client rule extracted as the pure `effectiveNavPermissions`, because it has **five** cases and three fail invisibly — *no session* keeps the full map (an empty set there makes real pages unreachable), while *a session holding nothing* and *a broken session* both fail CLOSED.
| ~~I79b~~ | ✅ **RAISED BY CODEX AND CLOSED 2026-08-25.** **MY OWN FIX FAILED OPEN, ONE FILE AWAY FROM MY OWN RULE NOT TO.** Authenticated with an active tenant absent from `/api/me` returned the FULL module map, justified in my comment as *“we do not know what this caller may do, so we do not pretend to”* — which sounds careful and is backwards, and directly contradicts `auth-provider.tsx`, where I had written that an API which cannot report permissions must show **LESS, never everything**. It is a BROKEN session, not an unknown one: every request in that state carries an organization header the API refuses, so the full map is a menu on which nothing works. Now the empty set, with the fifth state (authenticated, list empty) tested separately. **The fallback is for ABSENCE OF A SESSION, never for a broken one.** |
| ~~I79c~~ | ✅ **RAISED BY CODEX 2026-08-25, AND THE MEASUREMENT SAYS CODEX WAS WRONG — BUT ITS REASONING WAS RIGHT.** It argued `roles` might not be byte-identical because `array_agg(DISTINCT r.code)` carries no `ORDER BY` while 045's new one-to-many joins change aggregate input ordering. Measured on PostgreSQL 16.14: DISTINCT aggregation **must sort to deduplicate**, so reversing the input leaves the output identical while a plain `array_agg` follows its input. It does not reproduce. **However that is an implementation behaviour, not a documented contract**, and this project's rule is that a guarantee must be a MECHANISM rather than an ARGUMENT — the `# noqa`-as-safety lesson of 08-22. Both aggregates now state `ORDER BY` explicitly. |
| **I110** | 🔴 **OPEN. `SECURITY.md` §13 STATES A CONTENT-SECURITY-POLICY THAT DOES NOT EXIST.** The line reads *"HSTS, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, and a Content-Security-Policy without `unsafe-inline` scripts"* — as configuration, not as intent. The other four ARE real (`app/main.py:207-211`, `Caddyfile:81-83`). The CSP is not: measured independently by Codex and the Supervisor across `infrastructure/compose/Caddyfile`, `next.config.mjs`, `render.yaml`, `.github/` and the live response headers, and the only hits anywhere are inside Next.js's own build artifacts. **A security document asserting a control nothing implements is this project's most-repeated defect class, in the one file where it costs most** — a reader planning a deployment would believe the header is there. ⚠️ **Two separate things are owed:** correct the claim, and decide whether to add the header. If it is added, the pre-paint theme script (`app/layout.tsx`, inline by necessity in a static export) needs its `sha256-` hash in `script-src` — otherwise the browser blocks it, the dark theme silently stops applying before hydration, and **nothing fails**: the flash simply comes back. Raised by Codex as a P2 against the script; the script is not the defect. | P2 |
| **I111** | ⚠️ **OPEN. `next build` REWRITES `apps/web/tsconfig.json` ON EVERY RUN, AND IT HAS BEEN COMMITTED THREE TIMES.** Next reorders `include` by whichever `NEXT_DIST_DIR` last built. The third instance (`0ea7709`) carried `.next-review2/types/**/*.ts` — a path from the **Supervisor's** verification build, a directory that exists on nobody else's machine. Reverted by hand each time, which is a reflex and not a fix. 🔴 **It is a decision, not a cleanup:** `include` carries real entries (`**/*.ts`, `next-env.d.ts`, `.next/types`, `.next-demo/types`), so gitignoring the file takes those with it and a fresh clone gets no path aliases. The options are a committed `include` the build is permitted to reorder (and a pre-commit hook that normalises the order), or a build invocation that does not rewrite it. Nobody has measured which. | P3 |
| **I112** | ✅ **CLOSED 2026-08-28** by `apps/api/tests/auth/test_competitor_routes.py` — 13 cases driving the real app through FastAPI's dependency graph with real tokens. **Each of the three defects that had been green was reproduced by reverting its fix in the source**, then restored: the `test.view`-only benchmark gate, the missing `_translate` branch, and `verify_evidence`'s absent `guarded_write` (which reproduced the original 500 with a CheckViolation traceback). The permission guard is **structural** — it parses every `@router.post` with its own handler and fails when a write route's permissions all end in `.view`, so a route added later is covered without anyone remembering the file exists. apps/api 889 → 902. | P2 |
| ~~I100~~ | ✅ **CLOSED 2026-08-25.** **THE LIVE SUITE REPORTED GREEN WHILE MOST OF IT NEVER RAN, THREE INDEPENDENT WAYS, AND NONE WAS A CODE DEFECT.** Every green number the script had ever printed depended on environment variables typed by hand; it exported none and checked none. Run as documented: **290 passed / 0 failed / 392 skipped** — a zero-failure report over 290 of 682 tests with sign-in unverified. **(a)** `tests/db/conftest.py` reads `TEST_DB_PORT` defaulting to 5432 while this host's database is on 55432; the session fixture times out and 341 tests vanish. **(b)** `sign-in.spec.ts` self-skips without `TEST_KEYCLOAK_PASSWORD` — *the test written on 08-24 to stop sign-in breaking silently does not run in the live suite that exists to catch it.* **(c)** `--project=api` does not exist in LIVE mode. **FIXED by making the script DEMAND what it needs, not by remembering to export it.** A preflight classifies each capability CONFIGURED / ABSENT / PARTIAL: PARTIAL is always a hard failure (nobody half-configures on purpose, and it skips exactly like absence while looking configured); ABSENT needs an explicit `--allow-partial`, which names every gap in the report. **Not defaults** — hard-coding 55432 would aim the suite at whatever database the author had in mind. Two probes separate absence from misconfiguration: a database answering at an address the run is **already configured to use** (parsed from `DATABASE_URL`, not a guessed port list) while db-suite is unset → FAIL, unwaivable; db-suite set but nothing answering there → FAIL, which is the exact 290/0/392 shape caught in three seconds. Post-run: pytest's `-rs` reasons are printed, a CONFIGURED capability that skips anyway is counted FAILED, the Playwright projects that ran are read from Playwright's own report, every skipped e2e test is listed by file, and the sign-in guard asserts **both** that no test in that spec skipped **and** that at least one RAN — so a dead parser fails instead of passing silently. Capability-level skips are now NAMED in the report: `1 skipped` stood for 682 absent tests. **Falsified in eleven directions**, including a listener on 127.0.0.1:15432 to prove the derived probe reaches an address no port list would. |
| ~~I74~~ | ~~**The knowledge tier has NO PRODUCTION WRITE PATH.**~~ ✅ **CLOSED 2026-08-22.** `POST /api/knowledge/documents` (`knowledge.ingest`), `GET /api/knowledge/documents` and `GET /api/knowledge/search` (`knowledge.view`), plus migration **043** giving `knowledge.ingest` its first holder — it had been an explicitly allowlisted ORPHAN in `tests/db/test_002_roles_permissions.py` since migration 002, noted "Slice 8", exactly as `test.confirm` was before 019. That allowlist **asserts in both directions and turned the suite red** the moment the grant landed, naming `knowledge.ingest`; the entry is removed and the allowlist is now empty. Exercised end to end against the real database: ingest → list → search → MSD quoting it with `knowledge_document` evidence, and a chemist 403'd on POST while 200 on GET. ⚠️ Ingestion takes **TEXT, not a file** — no upload, no PDF extraction, no malware scan; those belong to I41's pipeline and `storage_key` is deliberately not settable from the body. **Original text:** | Slice 8 (`8c82083`) built `knowledge.documents` / `knowledge.chunks`, the authorization trigger, RLS, the embedder and `search_knowledge`. **Nothing writes to it.** `ingest_document` is called only from `tests/db/test_042_knowledge_retrieval.py` — no API route, no CLI, no job. On the deployed instance the table is permanently empty, so the `knowledge_search` branch ALWAYS falls through to the refusal and the only observable effect of the whole slice is one extra round-trip per unrouted question. Found by the Supervisor, and it is the standing **"which production path WRITES it?"** check that has now caught six things. ▶ Next slice: an ingestion route + the `/knowledge` screen (I75). ⚠️ Do not describe the RAG as shipped to the owner until this closes. |
| ~~I75~~ | ~~**No `/knowledge` screen.**~~ ✅ **CLOSED 2026-08-22.** `apps/web/app/knowledge/page.tsx` — search plus the document list, `LiveOnlyPage` with **no demonstration fixture** (a fabricated "standard" would reach an MSD answer wearing a real document's clothes). The conductor's `href="/knowledge"` is **restored in the same commit as the screen**. Nav needed `BUILT_AHEAD`: slice 8 shipped before 6 and 7, and raising `CURRENT_SLICE` to 8 would have turned Messages, Analytics, Failures and Approvals into live links into 404s. Copy says matching is on **shared words, not meaning**, and a zero-chunk document renders as "0 — not searchable" rather than a bare 0. **Original text:** | `apps/web/app/` has no `knowledge` directory. The knowledge answer emitted `href="/knowledge"` — a "go here next" link that 404s, which makes a working answer look broken. **The href has been REMOVED** rather than left pointing at nothing; it comes back with the screen. Pairs with I74: the screen is also where a document would be uploaded. |
| 🟠 **I76** | **HNSW recall can collapse to zero for restricted users, silently.** | The index yields ~`hnsw.ef_search` candidates (default 40) and RLS filters THOSE — so if the 40 nearest chunks in an org all sit in projects the asker is not a member of, `retrieve` returns nothing while a permitted, relevant passage exists further down. **No leak** — it fails closed — but the asker gets the ordinary "found nothing" refusal and nobody can tell the difference. Migration 042's header now states this correctly (it previously claimed rows were filtered "before ORDER BY", which is not how an HNSW scan executes). Fix when the corpus justifies it: raise `hnsw.ef_search` for this query, or exact-scan. **Measure, do not guess.** |
| 🟠 **I77** | **The knowledge relevance threshold is calibrated on six queries.** | `MAX_DISTANCE = 0.74` in `app/agents/tools/knowledge.py` sits in a measured gap between related (0.496–0.719) and unrelated (0.767–0.816) queries against the LEXICAL embedder. It stops "thoughts on the weather" being answered with four quoted CONFIDENTIAL-tier passages, which it was. It is **not** a relevance guarantee, and a neural embedder has a completely different distance distribution — the number **must be re-derived, not inherited**, when ADR-013's model is installed. |
| ~~I5~~ | ~~`record_decision` writes `testing.test_decisions` directly instead of driving `workflow.approval_routes`.~~ | ✅ **CLOSED 2026-08-21** (`a156d73`, `32af15b`, `1a0347e`). The defect was worse than described: the authority level came FROM THE REQUEST, so **one call approved a test outright at whatever authority the caller named** — a qualification test needing FOUR signatures was completed with one. §9's ladder was advisory. Review now OPENS a route keyed on the TEST's authority; approvals resolve the caller's next eligible step and go through `decide_step`; ADR-019 is carried as data (`must_differ_from_group`) and `_refuse_conflicted_approver` is deleted. 🔴 **The traffic light is untouched** — `approval_state` stays a STORED column with ONE writer, because §10's rules 3/11/12 read it and deriving it on read would be a second implementation of the mapping. ⚠️ **14 further defects were found by the two reviewers**, 6 by Codex and 8 by the Supervisor, **with no overlap** — including a returned rung that let the ladder be climbed past it, a correction that wedged a test permanently, and an authority level (`controlled`) that could not be reviewed at all. See I37–I39. |
| ~~I6~~ | ~~`open_failure_for_failed_test` has no caller.~~ | ✅ **CLOSED 2026-08-21** (`b4f8cf4`, `50a07ab`, `5c10ef0`). Called from `complete_execution`, the only writer of `calculated_result` and therefore the only place a RED result comes into existence. Six tests, and the ones that matter are the negatives: a PASSING confirmation opens none, and a failing **SCREENING** opens none — the distinction the rule actually turns on, which an implementation keyed on `calculated_result` alone would fail. 🔴 Wiring it exposed that **`open_failure` called `session.rollback()`**, which rolls back the TOPMOST transaction — a duplicate failure code would have destroyed the caller's completion and its audit event. Now a SAVEPOINT (`begin_nested()`, placed OUTSIDE the `try` so a pre-savepoint flush error is not misreported). ⚠️ My first test for that could not fail; proved by falsification against the old implementation after Codex caught it. |
| **I7** | **`revise_version` never writes `formula_version_drivers`.** | So "which failure caused this revision?" has no answer — a hole straight through the digital thread. |
| **I8** | **Notifications have no producer outside mentions.** | `notify()` is the single writer and only `_resolve_mentions` calls it. Approvals, failures and task assignment should all notify. §11 sidebar counts are actionable-item counts and will read zero. |
| **I9** | **CI seed gate does not cover `laboratory.*`, `testing.*`, `quality.*`, approval or messaging tables.** | The gate counts what the seeder wrote for Slices 1–3 only, so a seeder that silently stopped writing Slice 4–7 data would still pass. |
| **I18** | **No rate limiting of any kind exists in the API.** | Founder requirement (`ITWRD App.txt` §69 "rate limiting") and `SECURITY.md` §10, which described Valkey-backed per-user and per-IP limits in detail. Measured 2026-08-20: **zero implementation** — no middleware, no dependency, no counter. Raised independently by Codex and the audit's route sweep. 🔴 **Blocked on ONE decision that is not a code decision:** what the limiter keys on. Behind Caddy, `request.client.host` is the proxy for every caller — one bucket for the whole internet, which fails closed on the first burst; trusting `X-Forwarded-For` instead lets an attacker mint unlimited keys and defeats the limit. Needs the deployed topology (how many hops, which trusted). **A limiter keyed wrongly is worse than none.** Not currently exploitable for disclosure: the API is not deployed (I13/B4) and has no anonymous read or write path. Full reasoning in `SECURITY.md` §10. |
| **I19** | **`core.rls_permissive()` is still `SELECT TRUE`, so the database is not independently fail-closed.** | Every RLS policy opens completely when no request context is set; the only thing preventing a cross-tenant read is `session_scope()` raising in Python. `SECURITY.md` §1 requires any ONE layer failing not to expose data, and today the application layer is the *sole* enforcement whenever the GUC is absent. Deliberate scaffolding — the seeder, migrations and backfills all run with no GUC — so closing it **is** the FORCE-RLS cutover and needs its own migration and review. `tests/db/test_024_memberships_for_subject.py` fails the moment it lands and says what to do. |

### 🟠 P2 — what the live endpoints could NOT answer (opened by S2)

Each of these is stated on the screen itself, not hidden. They close when
a detail route or a richer endpoint exists — none is a defect in the
wiring.

| # | Gap | Detail |
|---|---|---|
| **I14** | **Suppliers: sole-source risk is not computed live.** | `GET /api/suppliers` returns `material_count`, not the material names, so "what breaks if this supplier fails" — the entire point of the screen, and a live risk (RSK-014-01, glass microspheres) — cannot be derived. The page carries a `role="note"` saying the analysis was NOT run, because a supplier showing no flag must not read as "not sole-sourced". Needs a supplier detail route. |
| **I15** | **Formulations index shows the LATEST version, not the current APPROVED one.** | `list_formulas` orders by `version_number DESC`. §8 makes revisions additive, so the newest is often an unapproved draft — which the old fixture page deliberately refused to lead with. Mitigated by always rendering that version's own badge (a draft says DRAFT). A correct "current approved version" needs a query per formula or a new endpoint. |
| **I16** | **Formulations index no longer shows computed figures.** | Theoretical density, solids, VOC, binder:filler and cost need `/versions/{id}/evaluation` — one call per version. They belong on the formula detail screen. §4 forbids the browser deriving them. |
| **I17** | **Projects list no longer shows gate progress, requirement counts or lead.** | `ProjectSummary` returns the project's own columns. Those three live on `/dashboard`, `/requirements/matrix` and `/members`; a list of forty projects must not run forty sub-queries. They belong on the project detail screen. |

### 🔴 P1 — found by adversarially reviewing migration 025

| # | Issue | Detail |
|---|---|---|
| ~~I20~~ | ~~`projects.project_members` has a `USING` clause and NO `WITH CHECK`.~~ | ✅ **CLOSED 2026-08-20 — migration 027** (`24c5917`). One INSERT made `core.is_project_member()` answer TRUE, and every project-scoped policy reads `confidentiality='normal' OR is_project_member(p.id)` — so one row opened formulas, batches, tests, failures, approvals and messaging. The `WITH CHECK` bootstraps off `projects.projects.lead_user_id`, which already existed and which migration 006 already used for exactly this purpose on the read side. 🔴 I had recorded this as *unfixable without a database* and was wrong — read the adjacent migration before declaring something impossible. ⚠️ The first escalation test **matched zero rows**, so no `WITH CHECK` was ever evaluated and it could not fail; fixed in `a5ac4e0` with `pytest.raises`. |

| **I21** | **A `technical_thread` channel may carry `project_id IS NULL`, and is then visible organization-wide.** | `022`'s `channels_project_channel_has_a_project` CHECK constrains only `channel_type = 'project'`, and `POST /api/messaging/channels` accepts `technical_thread` with a null project. `thread_for_record` is find-or-create keyed on `(entity_type, entity_id)`, so somebody holding a restricted record's UUID could pre-create an org-visible thread for it and have later "discuss this" clicks land there. Pre-existing, not touched by the 2026-08-20 audit, and the precondition — knowing a restricted record's UUID without being able to read it — is what keeps it below the fix-now bar. The fix is a CHECK constraint requiring `project_id` on `technical_thread` too. Raised as N1 by the Supervisor. |

### 🔴 P1/P2 — raised 2026-08-22 by the ITW Evercoat 23 extension and its review

Full detail in `IMPLEMENTATION_PLAN_EXTENSION.md` (v2) and
`docs/REVIEW_EXTENSION_ADJUDICATION.md`. Codex FAILED v1 with 30 findings
(3 BLOCKER); the Supervisor added 11 more with **one** overlap between them.

| # | Issue | P |
|---|---|---|
| ~~**I41**~~ | ✅ **CLOSED 2026-08-22** (`731e480`, `bf07e69`, `3dbd7b6`). Migrations **036** (a document reaches `approved` only with a checksum, a byte size, `scan_status='clean'` and a NAMED scanner+version, by CHECK constraint) and **037** (`materials.usable_documents` — ONE definition, because four queries in two modules counted these rows, including the one that BLOCKS submission and the one that TELLS a chemist whether it will). Pipeline: `app/core/file_types.py` (size → extension → **magic bytes** → declared type), `app/core/malware.py` (🔴 **an unavailable scanner RAISES**; the route answers **503, not 201** — there is no "scanning disabled" mode), `app/core/object_storage.py` (the store computes the checksum from the bytes it WROTE; callers cannot supply one). Route replaced JSON→multipart, ports injected via `Depends`. Seeder and golden scenario now write real bytes — both previously inserted dangling rows, so the acceptance artefact **canonised the broken model**. 🔴 `security_invoker=true` on the view is load-bearing: without it the view runs as the RLS-exempt owner and is a cross-tenant read path. **Proved by falsification** — restoring the permissive view fails both guards. ⚠️ Three existing guards fired and all three were right (view ownership; the boto3 ban, now narrowed to *enforce* `endpoint_url` rather than ban a name; the composite-key check, which read `information_schema.columns` and so included VIEWS). ⚠️ CI then caught three more: **`python-multipart` was never declared**, **constructing the object store touched the filesystem** so it failed at dependency resolution, and **the seeder stopped being idempotent** because `ON CONFLICT (storage_key)` cannot fire on a key containing a fresh UUID | ✅ |
| 🔴 **I41-was** | **The SDS safety gate counts ROWS, not files.** `POST /api/materials/{id}/documents` writes `material_documents` with a `storage_key`; **nothing writes the bytes** (`boto3` declared, never imported; no port, no adapter). So any holder of `material.edit` satisfies the control the golden scenario exists to demonstrate with `storage_key = "sds/anything.pdf"`. ⚠️ `seed.py:412` and `test_golden_scenario.py:236` both insert such rows, so tightening the gate alone makes every seeded formula permanently unsubmittable — a backfill and a byte-uploading seed ship with the fix | P1 |
| 🔴 **I42** | **The AI Model Router must not exist before the evidence-classification gate.** Building it first creates the bypass, then asks for a guard | P1 |
| **I43** | **`formula.export` does not exist** — export is not separable from read, not logged, and cannot require a second approval | P2 |
| **I44** | **Audit records no IP and no session id.** Cheap now, unrecoverable retrospectively | P2 |
| 🔴 **I45** | **Neon compatibility of FORCE RLS, five roles and `SET LOCAL` over the pooler is unverified.** ⚠️ And `tests/db/conftest.py` **skips** on any connection error, so "run the suite against Neon" can report 0 failed while testing nothing | P1 |
| **I46** | **No storage-quota monitor**, so Neon's 0.5 GB ceiling has no early warning | P2 |
| **I47** | **`RESUME_HERE.md` and I13 below both say no deployment path exists.** They become wrong the moment ADR-028 lands | P2 |
| ~~**I48**~~ | ✅ **CLOSED 2026-08-22** (migration 039). One lattice, as DATA with a rank rather than a six-literal enum, so *"at most PUBLIC"* is a comparison instead of a level list repeated at every query site: `PUBLIC < INTERNAL < CONFIDENTIAL < R&D_RESTRICTED < FORMULA_RESTRICTED < DIRECTOR_CONTROLLED`. §31's `MASTER FORMULA` maps to `FORMULA_RESTRICTED`. **Unset is the CEILING** — anything created without a decision is maximally restricted. 🔴 **Classification is not an access group**: `projects.confidentiality` stays a separate column because it answers *"is membership required"*, a different axis; a test asserts the two never merge. The **backfill is a stated decision** (formulas → `R&D_RESTRICTED`, supplier documents → `INTERNAL`, regulatory → `CONFIDENTIAL`), because applying the ceiling to existing rows would be "safe" and would empty the application the moment a read filter landed | ✅ |
| ~~**I43**~~ | ✅ **CLOSED 2026-08-22** (migration 039 + `export_version` + `GET /api/formulations/versions/{id}/export`). `formula.export` exists and is **separate from read** — §31 requires view/edit/approve/release/export to be five permissions and one of the five did not exist, so the rule was unimplementable rather than unenforced. 🔴 **Deliberately NOT granted to the Director** (§31: seniority is not a reason; export is the exfiltration act) **nor to the Chemist**, who may read and edit a formula and may not take it out — the separation only means something because it is asymmetric, and a test fails if a later migration grants it "for convenience". Every export is audited **including refusals**, with the classification and component count, because a refused attempt to remove a master formulation is the event a security review most wants to see. Above `R&D_RESTRICTED` one person may not export alone. **Proved by falsification** two ways | ✅ |
| 🔴 **I68** | **`formula.export` controls ONE door in a building with several — and my first docstrings claimed otherwise.** MEASURED: `GET /versions/{id}` requires only `formula.view` and returns the complete component list with percentages; `/evaluation`, `/weigh-up` and `/comparison` reconstruct it from other angles; MSD answers under `msd.use`. **The honest scope statement, now in the code and asserted by a test:** *`formula.export` authorizes and audits use of the dedicated export endpoint. It does NOT prevent or audit removal of formula information by anyone who can already view it.* Closing that needs classification enforced on every output channel, not a permission on one route — an export-safe representation, or entitlement + watermarking + response auditing + anomaly detection. Raised by Codex as a BLOCKER against my own overclaim | P1 |
| 🟡 **I69** | **HALF CLOSED 2026-08-22** (migration 041). 🔴 **The recipe subtree now resolves a label by INHERITANCE** — `formulations.effective_classification(version_id)` resolves through the parent formula, so `formula_versions`, `formula_components`, `formula_version_drivers`, `laboratory.batches` and `quality.failures` are covered without a column each. A column per table would be five copies of one fact, and a child saying INTERNAL while its formula says FORMULA_RESTRICTED is a **disclosure**, not a distinction — a test fails if any inheriting table grows its own column. Reclassifying the formula reclassifies everything derived from it in one instant, which is also what makes I49's purge implementable. **Invoker rights, not SECURITY DEFINER**, so it answers only for a formula the caller can see (a definer version would be I56's cross-tenant oracle). ⚠️ **STILL OPEN, and this is why the issue is not closed:** `testing.tests`, `test_replicates`, `laboratory.samples`, `messaging.messages` and `ai.msd_turns` hold recipe-derived content with **no FK into formulations**, so they are neither labelled nor discoverable — they are listed by hand in `test_041_classification_coverage.py`'s `DEFERRED` map. ▶ **The gap can no longer widen silently:** the coverage test discovers formula-derived tables from `pg_constraint`, so a new one fails on arrival until somebody decides. Proved by falsification | P1 |
| 🔴 **I69-was** | **Almost nothing is classified, so the lattice is decorative outside one query.** Only `formulations.formulas` and `materials.material_documents` carry a level. Unlabelled and equally sensitive: `formula_versions`, `formula_components` (**the actual recipe** — formula identity carries the label while the composition does not), `formula_version_drivers`, `testing.tests`/`test_replicates`/`test_decisions`, `quality.failures` and its evidence tables, `laboratory.batches`/`batch_components`/`samples`, `messaging.messages`, and `ai.msd_threads`/`msd_turns`/`msd_evidence`. Needs label **inheritance and aggregation**, a rule that derived/cached/copied data is never less restrictive than its source, and propagation of reclassification and deletion through every replica. Raised by Codex | P1 |
| **I70** | **`evercoat_app` holds blanket UPDATE on the formulation tables**, so any defect or future update path running as that role can lower `FORMULA_RESTRICTED` → `R&D_RESTRICTED`, export, and restore it. Migration 040 added a governed `set_classification` with a dedicated permission and an audited old/new+reason, but the column-level grant is untouched and no trigger constrains transitions. Revoke column-level UPDATE and enforce allowed transitions in the database. Raised by Codex | P2 |
| **I71** | **The lattice is a ranking, not a full authorization model.** Sensitivity is totally ordered; access is also compartmented by project, customer, programme, jurisdiction, NDA and need-to-know. It cannot express a shared formula, a selected-project compartment, or a purpose/destination restriction. Keep the ordered scale and combine it with explicit compartments plus a policy decision over subject/resource/action/purpose/destination. Raised by Codex | P3 |
| **I72** | **The refusal names the exact classification**, so a compromised export-capable account can enumerate which formulas are highly restricted via 409. Return a generic refusal externally and keep the level in the audit record only; rate-limit and detect probing. ⚠️ Also: the `classification is None` fail-closed branch writes **no** refusal audit, contradicting "every refusal is recorded". Raised by Codex | P3 |
| **I73** | **The backfill classifies every existing formula `R&D_RESTRICTED` regardless of lifecycle**, which would place a released master exactly at the one-person-export ceiling. ⚠️ **Not reachable today** — measured: `formula_versions.status` is draft/submitted/approved/superseded and there is no released state, because release is Slice 18. It becomes reachable the moment the release command ships, so the rule must be derived from authoritative release status then, with ambiguous rows quarantined at `DIRECTOR_CONTROLLED`. Raised by Codex | P2 |
| **I67** | **A controlled export has no second-approval route.** `export_version` refuses above `R&D_RESTRICTED`, and there is currently no way to obtain the second approval — so a `FORMULA_RESTRICTED` export cannot happen at all. Failing closed on a released master formulation is the right default, but a permanently blocked path is the "safety check that could only say BLOCKED" shape this codebase has already shipped once. §9's approval engine exists (`open_route`/`decide_step`, migrations 020/031) and should carry a real two-person export route | P2 |
| 🔴 **I48-was** | **Two data-classification taxonomies** in the source (§31 has `PUBLIC` and `MASTER FORMULA`; §34 has `FORMULA RESTRICTED` and `DIRECTOR CONTROLLED`). The outbound AI gate is defined in terms of `PUBLIC`, which only one contains. One lattice, deny-by-default, unset = ceiling | P1 |
| 🔴 **I49** | **No retention or deletion anywhere.** Deleting a document leaves the content in embeddings, caches, graph edges and prior object versions. v3's F33 already promised purge-on-revocation; nothing implements it | P1 |
| **I50** | **Backup destination unnamed** — restic on the same tenancy with the same credentials is not a backup against account compromise or Oracle reclaiming capacity | P2 |
| 🔴 **I51** | **I18's proposed fix could not work.** `CF-Connecting-IP` restricted to Cloudflare IP ranges **never matches behind `cloudflared`** (the origin sees the local daemon), and Caddy is a second hop. Re-designed: Caddy as sole header authority, limits keyed on the subject claim where authenticated | P1 |
| **I52** | **Oracle capacity is 2 OCPU/12 GB in the source and 4 cores/24 GB in three repo documents**, and the number decides the Penpot exclusion and the local LLM's RAM | P2 |
| **I53** | **Oracle ARM caveats**: regional capacity is frequently unavailable, and the API image needs an ARM or multi-arch build. An `amd64`-only image fails after the host slice was signed off green | P2 |
| ~~I55~~ | ✅ **CLOSED 2026-08-23** (migration 044, `6cf3be5`). `core.users` now carries RLS: a row is readable only if the reader shares an organization with that user, or is that user. **Measured before: 571 rows** — not the 290 this entry recorded; the development database had grown, and the number was quoted from a handover rather than re-measured. **Measured after: 0** with no context set. `core.user_id_for_subject` was added in the same migration so multi-organization membership still works — 044's read policy makes an existing user in another organization invisible and `keycloak_sub` is globally unique, so without it an administrator could neither find nor create them. ⚠️ Membership `status` is deliberately NOT in the predicate: eleven INNER joins resolve an actor through `core.users`, so filtering would drop the records themselves out of every list, not merely blank a name. The other four non-RLS `core` tables remain deliberate global RBAC reference data (`roles`, `permissions`, `role_permissions`, `member_roles`), and `core.classifications` joins them — the 039 lattice, global by design | ~~P1~~ |
| ~~I80~~ | ✅ **RAISED AND CLOSED 2026-08-23** (`6cf3be5`), found while measuring I55. **`invite_member` wrote across tenants.** Its `INSERT ... ON CONFLICT (keycloak_sub) DO UPDATE SET display_name`, replayed as `evercoat_app` under organization A's GUC against a subject belonging only to organization B, **renamed that user** and the `RETURNING` **handed back B's real email address** even though the caller had supplied a different one. One statement behind `admin.users`, a cross-tenant write and a cross-tenant read. The route no longer upserts: it resolves the subject, inserts only when absent, never touches an existing row's email or display name, and returns the STORED values rather than the submitted ones. 🔴 **The migration's first comment said the UPDATE policy closes this and that "both policies are required". Both claims were false** — the matrix was measured, either policy alone refuses it, and a direct UPDATE with the UPDATE policy made permissive still changed 0 rows because PostgreSQL applies the SELECT policy to rows an UPDATE reads through its WHERE. The read policy does this work; the UPDATE policy exists so the table is not read-only | ~~P1~~ |
| ~~I83~~ | ✅ **CLOSED 2026-08-25.** Migration **046 / f1000**, **ADR-028**. **A CROSS-TENANT EMAIL EXISTENCE ORACLE, THROUGH A CONSTRAINT RATHER THAN A POLICY.** `core.users.email` carried a GLOBALLY unique constraint and unique constraints are enforced OUTSIDE RLS. Re-measured before fixing: as `evercoat_app` scoped to organization A, inserting an address held in organization B was **REFUSED by `users_email_key`** and an unused address **ACCEPTED** — 409 against 201, readable by any `admin.users` holder in any tenant with a throwaway subject and no row left behind. Squatting confirmed in the same run. **FIXED BY DROPPING THE CONSTRAINT, not by disguising its refusal**: 044 had already made the refusal generic and the oracle SURVIVED, because the attacker reads the status code, and a creating endpoint cannot make *created* indistinguishable from *not created*. Identity is `keycloak_sub`; email is an attribute. 🔴 **THE STATED COST DID NOT EXIST** — the @mention query matches `split_part(email,'@',1)` and the constraint was on the whole address, so it never protected it; two members of one organization at `chris@one.example`/`chris@two.example` already returned 2 rows into a `.one_or_none()`, **a latent 500 on posting a message**, fixed here (ambiguous resolves to nobody). Replaced by **two** SECURITY INVOKER constraint triggers — one on `core.organization_members` for the INSERT path, one on `core.users` for the rename path — both advisory-locked. 🔴 **THE REVIEW ROUND FOUND THE REPLACEMENT WAS NOT A CONSTRAINT, TWICE**: it was racy (two concurrent writers both committed, measured) and it was bypassed entirely by `UPDATE core.users SET email = ...` (measured — on that path 046 was briefly **weaker** than what it removed). Both closed and re-measured. **A guard that refuses on a row you cannot see would be the oracle again**, so the rename guard was proven to ACCEPT a collision in an organization the caller cannot see — it misses rather than answers, which ADR-028 states plainly. | ~~P1~~ |
| ~~I81~~ | ✅ **CLOSED 2026-08-25.** Migration **047 / f2000**, **ADR-029**. 044's read policy hands the whole row over where its justification — attribution in eleven joins — needs only the name. 🔴 **THE OBJECTION WAS MEASURED, NOT ACCEPTED, AND IT WAS TWO-THIRDS RIGHT:** `display_name` has eleven readers; `email` has **two production paths that deliberately return it** (`admin.list_members`, and `projects.list_members`, which documents that it lists FORMER members on purpose) so removing it would break stated behaviour; and `keycloak_sub` is read by **no application query anywhere**. Only the last is over-granted, and RLS cannot take a column away — column privileges can. SELECT and UPDATE on `keycloak_sub` revoked from all three runtime roles; INSERT kept, because `invite_member` creates identities. ⚠️ **A column-level REVOKE against a table-level GRANT does nothing** — the table grant must be replaced by an explicit column list, so the test asserts the PRIVILEGE, not the SQL. Sign-in unaffected (the three readers are owner-owned definers) and proven, not argued. 🔴 **The review round found I had granted a CROSS-TENANT WRITE while removing a cross-tenant read** — `UPDATE (…, status)` on a GLOBAL row would let one organization archive an identity out of every other; now `(email, display_name)`. Also hardened 046's rename guard, which was scoped by the caller's RLS rather than its own predicate and went **global inside a SECURITY DEFINER** (measured). | ~~P2~~ |
| ~~I102~~ | ✅ **CLOSED 2026-08-25.** **THE LIVE SUITE LOCKED ITSELF OUT OF KEYCLOAK AND BLAMED THE PASSWORD.** The realm sets `bruteForceProtected: true`, `failureFactor: 5`, and `test_auth_end_to_end.py` made **twelve** direct-grant calls per run for a handful of users. Measured: the client was told *"401 invalid_grant — Invalid user credentials"* with the CORRECT password while Keycloak's own log said `error="user_temporarily_disabled"`. 🔴 **Keycloak returns the same error for a lockout as for a wrong password**, so the suite whose job is three trustworthy numbers failed itself, misnamed the cause, and carried the lockout into the next run. **FIXED TWO WAYS**: `_token()` caches per username (twelve authentications of one user proved nothing that one does), and `live-suite.sh` clears the lockout before each run — deliberately **hygiene, not coverage**, so it warns and proceeds rather than failing the preflight, with admin credentials from the environment and never defaulted. The assertion now says *check the password LAST*. **Proven against the real broken state**: `lead.demo` was deliberately locked with six wrong passwords until the correct one returned `invalid_grant`, and the suite then ran **750/0/0** from exactly that. | ~~P2~~ |
| 🟠 **I82** | **`core.user_id_for_subject` hands out a uuid, not merely existence.** Raised by Codex. 044's comment first claimed it discloses nothing beyond what the UNIQUE constraint does; **that was false and is now corrected in the migration.** It gives up (1) the user's uuid, which matters because `project_members.user_id`, `tasks.assigned_user_id` and others are plain `REFERENCES core.users(id)` and referential integrity bypasses RLS — the hole `app/core/tenancy.py` guards **in Python**; and (2) existence on a SELECT, leaving no row behind, where the constraint answers only on an INSERT. Reachable only with `admin.users` and the exact subject string, and the route's next act binds that user anyway. **The narrower design — fold resolution into a single atomic bind so the id is returned only after the membership exists — changes the route's transaction shape and belongs with I83** | P3 |
| ~~I84~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **EVERY MEASUREMENT ON THE FORMULATION AND TEST-DETAIL ROUTES WAS A FLOAT.** `jsonable_encoder` maps `Decimal` to float, and `app/domains/formulations/service.py` had **no conversion helper of any kind** while `testing.get_test` built `statistics` and `automatic_evaluation` *after* its row-level `_decimal_strings` had run. Measured against the running service before the fix: `percentage 2.5`, `density_g_cm3 2.2`, `cost_per_kg 6.4`, and `theoretical_density_g_cm3 1.0906918323011936` — sixteen digits of binary-float noise where the engine had produced a `Decimal`. `CLAUDE.md` §5 — *"NUMERIC, never float, for percentages, masses, densities and measured values"* — was satisfied in the database, satisfied in the engine, and **satisfied nowhere in between**. 🔴 **THE ORPHANED ROUTE WAS HIDING IT.** This is the identical defect `tests/test_laboratory_testing_serialisation.py` was written for on 2026-08-19, whose own header explains it survived because *"no screen was wired to these routes yet, so no client had ever parsed the response"* — and the fix that day went exactly as far as the routes wired that day. Found the same way again: by wiring a screen to routes nothing had ever called. Fixed at the RESPONSE boundary only, never at load, because the engine needs real `Decimal`s. Pinned by five new tests | ~~P1~~ |
| ~~I85~~ | ✅ **RAISED AND CLOSED 2026-08-24**, found while fixing I84. **DERIVED PROPERTIES CARRIED FALSE PRECISION.** The engine deliberately does not round — it divides at full `Decimal` context precision — so with I84 fixed a theoretical density became the *string* `1.092376966584235260696368803`: twenty-eight significant digits asserted from inputs recorded to four. Rule 3 requires a theoretical density presented **as calculated**, not dressed up as measured. Quantized to `0.0001` at the response boundary (`PROPERTY_SCALE`), never in the engine — `test_formulation.py` asserts `binder_to_filler_ratio` is exactly `Decimal("40")/Decimal("60")` and the engine must go on answering that exactly. 🔴 **FOUR PLACES IS NOT INVENTED:** `scripts/build_demo_formulations.py` already quantizes these same properties to `"0.0001"` and stringifies them, and `test_msd_conductor.py` pins densities as `"1.5790"`/`"1.0920"`. The build-time fixture and the live API render onto the **same screens**, so a scale that disagreed would show one formula two different ways depending on where it was read from | ~~P2~~ |
| ~~I86~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE FORMULA LIST COULD NOT REACH THE FORMULA WORKSPACE, AND THAT IS WHY THE WORKSPACE DID NOT EXIST.** Twelve of the thirteen formulation routes are keyed by `version_id`; `list_formulas` returned the latest version's `version_code`, `version_number` and `status` and **not its `id`**. A `version_code` is a label — unique per formula, not per organization — so it is not a key. The browser therefore had **no identifier with which to open any version-scoped route**, and `/formulations/[code]` rendered `lib/demo/dataset.ts` instead. This was not a missing screen; it was a missing column that made the screen unbuildable, and the fixture quietly stood in for it. `latest.id AS latest_version_id` added to the LATERAL and the outer select; the zod schema's coherence refinement extended to four fields so a present id beside a null code fails loudly rather than producing a dead link | ~~P2~~ |
| ~~I87~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE DIFFERENCE ENGINE SHIPPED WITHOUT TWO OF THE COLUMNS THE PLAN NAMES.** §H Slice 3 specifies *"old / new / Δ / %Δ / reason / expected / observed"*. `compare_versions` returned `previous_percentage`, `new_percentage` and a categorical `change`, and its docstring explained the omission: the delta *"is a SUBTRACTION OF TWO PERCENTAGES and is therefore arithmetic — so it is not done here … the one place that may subtract them is the engine."* **The refusal was right and no such engine function was ever written**, so the gap was invisible behind a correct-sounding comment. `component_delta` added to `app/calculations/formulation.py` returning percentage-POINT and RELATIVE movement as separate named fields — 2.5%→5.0% is 2.5 points and 100% relative, and a reader will confuse them if either is unlabelled. `None` for an added or removed component (not zero, not its own percentage), and `percent_delta` `None` from a zero base, which is a division by zero rather than an infinite increase. Raised by Codex; six tests | ~~P2~~ |
| ~~I88~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **MSD HAD A COMPLETE MEMORY AND NO WAY TO READ IT.** `GET /api/msd/threads` and `GET /api/msd/threads/{id}/turns` both existed, were tested, and **neither had a caller anywhere in the application**. `MsdPanel` held its thread id in a `useRef` and its exchanges in component state, so closing the panel or reloading the page began an empty conversation — while `ai.msd_threads` and `ai.msd_turns` had been faithfully recording every exchange all along. A route with no caller, surfacing as a product defect a user would report as *"the assistant forgets everything"*. The panel now adopts the caller's most recent thread on open and replays it, folding stored turns back into question/answer pairs; **a replayed answer keeps the disclaimer read from its stored turn**, never a constant, so §7 holds on history exactly as it does live. "New conversation" is now an explicit control rather than an accident of navigation | ~~P2~~ |
| ~~I89~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE BATCH BENCH COULD DO EVERYTHING TO A BATCH EXCEPT CREATE ONE.** Ten of the eleven laboratory routes had a caller after 2026-08-23; `POST /api/laboratory/batches` did not. Every batch on screen had therefore been inserted by a seeding script, so the lifecycle was demonstrable only on records **no user could have produced** — the same defect as a table with no writer, one step earlier in the chain. Found by Codex while auditing the claim that Laboratory was complete, which it was not. `createBatch` added | ~~P3~~ |
| ~~I90~~ | ✅ **RAISED AND CLOSED 2026-08-24 — MY OWN CENTRAL CLAIM WAS FALSE, AND CODEX CHECKED IT.** Commit `a14c95f` stated that all 37 endpoints across the four modules had a production caller. `createBatch`, `createTest`, `createFormula` and `classifyFormula` existed **only as declarations in `lib/api/*.ts`** and were referenced nowhere else in the repository. **A client function is not a caller** — it is the same defect the commit exists to remove, moved one layer up, with a function standing where the person pressing something should be. Closed properly rather than by editing the claim: **`GET /api/testing/methods`** and **`GET /api/formulations/classifications`** were added first, because without them a planning form had no method to offer and a reclassification form had no level — the *"editable in Administration"* gap §H warns about, where a configuration value is referenced everywhere and read by nothing. Then the four controls were built where the digital thread already carries the identifier they need: **plan-a-test on each sample of the batch bench** (§5 — a result exists only against a specimen actually taken), **create-a-batch on the formula workspace** (§2 — a batch is made against a formula *version*, the one id the batch queue does not have), create-formula on the formulations list against a chosen project, and reclassify on the formula workspace | ~~P1~~ |
| ~~I91~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE SERVER'S SENTENCE WAS BEING THROWN AWAY WHILE FOUR SCREENS CLAIMED TO SHOW IT.** `apiRequest` throws `ApiError` with a generic message — *"the API refused this request (422)"* — and puts the server's own explanation in `.detail`, which **no screen read**. So the formula workspace rendered `error.message` under a comment saying *"the server's own sentence … explains why"* and displayed a status code; the test workspace did the same under *"a 403 is surfaced as the sentence the server sent"*, losing the ADR-019 segregation-of-duties distinction that is the entire reason a 403 there is interesting. **Worst of all, a blocked submission's blocks were discarded wholesale** — measured against the running service, `post_submission` returns `{"message": "this formula cannot be submitted", "blocks": [TOTAL_OUT_OF_TOLERANCE …, RESTRICTED_MATERIAL …]}`, and the chemist saw none of it, on a route whose own docstring says returning one block at a time *"is how a form teaches people to distrust it."* New `serverMessage()` in `lib/api/client.ts` handles all three detail shapes and renders **every** block. Raised by the Supervisor. 🔴 **This is the codebase's most-repeated defect — a comment asserting a rule the code does not have — and it was in four comments written in one sitting** | ~~P1~~ |
| ~~I92~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE ONLY UI PATH THAT CREATES A REVISION RETURNED 422 EVERY TIME.** `RevisionCreate` requires `change_reason`, `technical_hypothesis` **and** `driver_type`, none defaulted. `RevisionRequest` declared the first as required, the second as optional and the third **not at all**, so the type system could not catch it — and the control the workspace itself labels *"the only way a formula changes"* failed on every press. `driver_type` is undefaulted deliberately (§2: a revision must show *which* objective caused it; *"a default would answer the question on the chemist's behalf"*), so the form now collects all three. Raised by the Supervisor | ~~P1~~ |
| ~~I93~~ | ✅ **RAISED AND CLOSED 2026-08-24.** Four smaller defects, each a mechanism that looked correct and did nothing. **(a)** `invalidateQueries({queryKey: ["dashboards"]})` matched **no query in the application** — inert configuration under a comment claiming it kept the failure queue current, which is precisely the state it left behind; replaced with `["my-work"]`, which exists. **(b)** A failed `/evaluation` rendered **"Calculating…" for ever**: only `.data` was passed into the workspace and the hook's `error` was discarded, so a 403 or 500 read as a slow calculation while the rest of the page worked. **(c)** `submitVersion` posted a `note` to a route that **declares no request body**, so FastAPI ignored it and the caller got a 200 with nothing recorded — a parameter with no destination, removed rather than left as a promise the API does not keep. **(d)** `_try`'s `quantize` ran **outside** the guarded block, and `InvalidOperation` is not a `ValueError`, so the module's stated "a value or a stated reason" contract could become a 500. All raised by the Supervisor | ~~P2~~ |
| ~~I94~~ | ✅ **RAISED AND CLOSED 2026-08-24**, found while re-verifying I90 rather than by either reviewer. Three defects of my own making. **(a)** `GET /methods` was declared on the testing router, which `main.py` mounts at **`/api/testing/tests`** — so it answered at `/api/testing/tests/methods`, which reads as "the methods of a test", is not what it is, and 404'd the honest URL. A test method is reference data belonging to the module, not a sub-resource of any one test; it now has its own `reference_router` mounted at `/api/testing`. **(b)** `useMsdThreads` and `useMsdTurns` were written into `hooks.ts` and **nothing ever called them** — `MsdPanel` hydrates inside an effect because its unit is an *exchange*, not a rendered query, so it calls `fetchThreads`/`fetchTurns` directly. Dead code duplicating a live path; deleted rather than kept, because a second way to load the same data is how two screens end up disagreeing about what a conversation contains. **(c)** 🔴 Deleting that block also removed **`useWeighUp`**, which sat between it and the next marker — caught by `next lint`, NOT by the typecheck I had read as clean from an **empty output file that was still being written**. *An empty file is not a passing run.* | ~~P2~~ |
| ~~I96~~ | ✅ **RAISED AND CLOSED 2026-08-24 — EVERY BROWSER SIGN-IN ENDED ON A 404 AFTER AUTHENTICATING SUCCESSFULLY.** `Caddyfile.tunnel` routes `/auth/*` to Keycloak with the prefix stripped, but the application's OIDC callback **is** `/auth/callback/` — `lib/auth/config.ts` sets `CALLBACK_PATH` to exactly that, there is a real page at `app/auth/callback/page.tsx`, and that URL is what the realm registers as the redirect URI. So Keycloak was asked for `/callback/` and answered 404. Measured: `:3000/auth/callback` → **200**, `:18081/auth/callback/` → **404** — the application was fine and the **PROXY had no route to it**, the same shape as the `/health/*` fall-through fixed on 08-23, one directory along. ⚠️ **It had never surfaced because the demo had only ever been driven by DIRECT-GRANT tokens** (`evercoat-test`), which never touch the callback: *"proven by hand with real tokens"* was true every time and never meant sign-in worked. Fixed by matching `/auth/callback*` to the web tier **before** the identity prefix. Proven end to end: authorize → real login form → POST credentials → **302 carrying an authorization code** → callback **200** on the app's own page | ~~P1~~ |
| ~~I97~~ | ✅ **RAISED AND CLOSED 2026-08-24.** **THE INTERACTIVE SIGN-IN FLOW WAS COVERED BY NOTHING, AND I96 IS WHAT THAT COST.** The live suite read **713 / 0 / 0** while every browser sign-in returned 404: `api-live` authenticates by DIRECT GRANT and the e2e shell suite uses a seam compiled out of production builds, so **neither traverses `/auth/callback`**. `tests/e2e/shell/sign-in.spec.ts` now drives what a person does — press **Sign in**, land on the realm's authorize endpoint, fill the real Keycloak form, and return to the application with the Sign in button gone and the organization switcher present — plus a narrow guard that requires the APPLICATION (not Keycloak) to answer `/auth/callback/`, which needs no realm and no password. 🔴 **PROVEN IN BOTH DIRECTIONS, WHICH IS THE POINT:** 2 passed on the fixed stack; **2 failed** with the Caddy route deliberately removed, printing *"the OIDC callback did not reach the application — the identity proxy is almost certainly claiming /auth/\* including /auth/callback (I96)"*; and **1 failed** when the bundle lost its identity config, stopping at the first hop with the page snapshot naming the cause. Page status codes and all 713 other tests stayed GREEN through every one of those broken states. ⚠️ The password comes from `TEST_KEYCLOAK_PASSWORD` (never hard-coded — this repo is public) and the file states that **a skip here is a gap, not a green** | ~~P1~~ |
| ~~I95~~ | **`apps/web/app/knowledge/page.tsx` still renders `error.message` in three places** (lines ~380, ~478, ~521) and therefore still shows *"the API refused this request (…)"* instead of the server's explanation. Same defect as I91, same one-line fix (`serverMessage`), **deliberately not made in this session** because Knowledge is outside the four modules under review and a silent drive-by edit to an unreviewed module is how scope stops being measurable. Named here so it is a gap rather than an oversight | P3 | ✅ **CLOSED 2026-08-25** (`4ceeefd`) together with I98 — all three sites now call `serverMessage`, and the fix went in with the component test that proves it, rather than as the one-liner it looked like.
| ~~I98~~ | ✅ **RAISED AND CLOSED 2026-08-25.** **I91 WAS FIXED ON THE WRITE PATHS AND MISSED THE SHARED READ PATH, WHICH IS THE BIGGER HALF.** `apps/web/components/ui/data-source-banner.tsx` rendered `{error.message}` and is called from **fifteen sites across eleven screens**, so every failed READ in the product reported a status code and discarded the server's sentence — including a blocked submission's entire block list. 🔴 **Codex was asked this exact question on 08-24 and answered NONE**, because it matched the literal `.error.message` while the component reads `{error.message}`; the Supervisor pass found it. Also fixed `components/msd/msd-panel.tsx`. 🔴 **AND THE REASON IT COULD HIDE: NO COMPONENT IN THIS APPLICATION HAD EVER BEEN RENDERED BY A TEST** — `vitest.config.ts` was `environment: "node"` with no React plugin and there were **zero `.test.tsx` files**, while `@testing-library/react`, `jsdom` and `@vitejs/plugin-react` had been devDependencies all along. Wired, then proved by falsification (unfixed component → 2 failed). |
| ~~I98b~~ | ✅ **RAISED AND CLOSED 2026-08-25 — THE FIX FOR I98 INTRODUCED A REGRESSION, AND BOTH REVIEWERS CAUGHT IT.** `serverMessage` mines `error.detail`, but **`detail` is not always a response body**: `ApiUnreachableError` stores the caught `TypeError` there and `ApiShapeError` stores a `SyntaxError`/`ZodError`. All are objects carrying `.message`, so the shared component began rendering the browser's raw **"Failed to fetch"** in place of *"the API could not be reached"* — the sentence `client.ts` produces deliberately because a CORS refusal and an offline network *"mean the same thing to the reader … reported as one state rather than guessed between"*. On a tunnelled demo that is the MOST COMMON error there is. Guarded in `serverMessage` so every future caller inherits it; a test per class; proved by falsification (guard removed → 1 failed). |
| ~~I99~~ | ✅ **RAISED AND CLOSED 2026-08-25 — `scripts/demo-up.ps1`, THE ONE COMMAND THE WHOLE HANDOVER RESTS ON, WAS BROKEN TWO WAYS AND ONE OF THEM HAD NEVER RUN.** **(a)** Line 207 held a literal **BEL byte (0x07)** where `` belongs — `Set-Location '$RepoRoot<BEL>pps\web'` — introduced by `c98290f` at 08-24 12:52, **four minutes after the last successful web build at 12:48**, so the standalone web path in the committed script had never once executed. **(b)** The Keycloak client repoint **could not work from PowerShell 5.1**: `-s "redirectUris=[\`"…\`"]"` passes embedded double quotes to a NATIVE exe, the `CommandLineToArgvW` round-trip strips them, and `kcadm` answered `Cannot parse the JSON [unknown_error]`. Reproduced with a single-element array, so it is the quoting and not the length. Now a `docker cp`'d ASCII JSON file applied with `-f`. **The read-back guard is what caught (b)** — it refused to report success and printed the client still holding the previous tunnel's hostname. |
| ~~I99b~~ | ✅ **RAISED BY CODEX AND CLOSED 2026-08-25.** **THE READ-BACK GUARD WAS WEAKER THAN IT READ.** `$stored -notlike "*$PublicUrl*"` never looked at `webOrigins` (so a CORS failure passed as *verified*), passed if the hostname appeared **anywhere** (so three of the four redirect URIs could be missing), and with `-TunnelName` — where the hostname is **stable** — would report success on the previous run's value after a wholly failed update. Worse, `-like` made one expected value, `$PublicUrl/*`, a **pattern** whose trailing `*` matches anything, so that entry could not fail. Now asserts all six values with ordinal `.Contains()`, plus the `docker cp` and `kcadm` exit codes. *A check that cannot fail is not a check* — found one level inside a guard written to enforce exactly that. |
| 🔴 **I61** | **The database validates the SHAPE of document evidence, not the evidence.** MEASURED 2026-08-22: as `evercoat_app` with a tenant GUC, `INSERT ... status='approved', scan_status='clean', checksum_sha256=repeat('a',64), scanner_name='totally-real-scanner', storage_key='no-such-key-forged'` was **accepted and counted by `usable_documents`**. 036's header claimed a row "cannot claim a file the store does not hold" — true of the SERVICE path, false of the database. **038 closed the UPDATE half** (evidence columns are write-once, so a `legacy_unverified` row cannot be promoted and safety history cannot be rewritten). The INSERT half remains: PostgreSQL cannot verify an object store. Fix = revoke direct INSERT and route every write through one audited function, so the surface is one reviewable place. Raised by Codex | P1 |
| 🔴 **I62** | **Nothing re-verifies that an approved document is still retrievable, or unchanged.** The view proves storage by COLUMNS, never by asking the store — so deleting or replacing the bytes after approval leaves the row usable indefinitely, and the checksum is never re-computed. Measured: deleting the store and re-seeding took the usable count 11 → 22, one live document per material plus one pointing at nothing (the seeder now supersedes, which is a seeder-local mitigation of a general defect). Needs a reconciliation worker that re-reads each approved object, re-computes its checksum, and quarantines rows whose bytes are missing or changed. Raised by Codex | P1 |
| 🔴 **I63** | **"Scanned clean" is not "is a Safety Data Sheet".** A PNG, a JPEG, a UTF-8 text file, a DOCX or an XLSX can be uploaded with `document_type='SDS'` and satisfies the gate. The pipeline validates a generic file type and malware, never regulatory validity. Also: a `%PDF-` prefix is sufficient, so a PDF carrying `/JavaScript`, `/OpenAction`, embedded files or launch actions is accepted; `.docx`/`.xlsx` accept any ZIP signature, so a ZIP bomb or an encrypted archive passes; and arbitrary UTF-8 is accepted as `.txt`, including HTML/SVG with scripts. Needs an SDS-specific document policy plus a human QA approval step, structural PDF parsing with active-content rejection, OOXML entry verification with expansion limits, and forced-attachment download for text. Raised by Codex | P2 |
| **I64** | **`MAX_UPLOAD_BYTES` is enforced AFTER `await file.read()`** has consumed the whole request into memory, so the 25 MB limit does not bound request memory and concurrent oversized uploads can exhaust it. Stream into a bounded spool while counting, reject above the limit immediately, and set a body limit at the proxy. Raised by Codex | P2 |
| **I65** | **Storage and database work are not one recoverable unit.** Cleanup covers `IntegrityError` and an invisible material, but an unexpected DB error, an audit failure, a cancellation or a crash after `store.put()` leaves an orphan object — clean-scanned, so not an unscanned-file path, but invisible to every retention and inventory control (I49). Needs a pending/outbox workflow plus reconciliation. Raised by Codex | P3 |
| **I66** | **037's supersession rule is incomplete.** Any approved child suppresses the parent even if the child is expired, so an expired replacement makes every revision unusable — fails closed, but is an avoidable document outage. The child also need not reference the same material or document type; the FK guarantees only the organization. And the correlated `NOT EXISTS` has no supporting `(supersedes_id) WHERE status='approved'` index, so on the formula-submission hot path it can scan the document table per candidate. Raised by Codex | P2 |
| **I56** | **`core.is_project_member` is SECURITY DEFINER owned by `postgres` (a superuser).** Measured. A superuser-owned definer function is outside RLS **permanently, including after FORCE** — so the claim that FORCE will govern all access is not true while it stands. Its body is a narrow fixed-search-path boolean today, so it is not a data extractor; the danger is that ownership makes any future edit to it unbounded. Re-owner to a dedicated NOLOGIN lookup role with minimum privileges, revoke PUBLIC, grant only its callers. Raised by Codex | P2 |
| **I57** | **`audit.events_id_seq` is selectable and usable by the app/worker roles.** RLS does not protect sequences: `last_value` leaks global audit volume across tenants and `nextval` lets one tenant consume another's identifiers. Not a formulation disclosure, but a cross-tenant metadata channel. Prefer an identity column with minimum privilege; test sequence grants explicitly. Raised by Codex | P3 |
| 🔴 **I58** | **The FORCE RLS cutover — I19 is HALF done and must not be described as finished.** 032 closed the permissive branch for ordinary runtime queries; the owner exemption means every owner-owned definer routine is still outside RLS, and a superuser-owned one (I56) is outside it permanently. Codex supplied an 11-step specification the FORCE migration must satisfy **atomically**: inventory and classify every relation (tenant-scoped / deliberately global / migration metadata) · FORCE on every tenant-scoped table · remove direct grants on the identity tables or justify policies · replace superuser-owned definers · create dedicated NOLOGIN roles for the principal, membership, project-membership and audit-chain lookups · grant those roles minimum table privileges · re-owner each definer to its role, pin `search_path`, revoke PUBLIC, grant only intended callers · rework `chain_row`/`memberships_for_subject`/`principal_for_subject` to survive FORCE **without** giving the general owner universal bypass · assert no runtime/report/worker/breakglass role is superuser, owner or BYPASSRLS · test every table and definer as the real app/worker/report/breakglass roles across unscoped, wrong-tenant, inactive-identity and scoped-positive cases · test migration/backfill and seeding under their actual deployment credentials. **The tripwires in `test_024` and `test_011` fire when it lands and say what to do** | P1 |
| **I59** | **No adversarial test of `core.principal_for_subject` as the runtime role.** It is an owner-bypass API at a security boundary and `test_sign_in_still_works` exercises a *different* function through `owner_session`. Needs, as `evercoat_app`: valid pair succeeds · wrong organization returns zero · unknown organization returns zero · inactive user returns zero · inactive membership returns zero · worker/report/breakglass cannot execute (now true, untested) · a caller-controlled `search_path` cannot redirect it. Raised by Codex | P2 |
| **I60** | **`test_032` samples 4 of 59 tables and asserts nothing about the catalog.** It passes while the five unprotected `core.*` tables remain readable (I55). Replace sampling with a catalog-driven test that classifies **every** application relation and asserts RLS, policies and grants per class, plus `rolsuper = false` and `rolbypassrls = false` on all five roles. Raised by Codex | P2 |
| **I54** | **~14 of the source's 24 research tables have no owning slice** — needs a table→slice ownership matrix or an explicit rejection per table | P2 |

▶ **Shipped 2026-08-22 — I19 CLOSED** (migrations 032/033/034, `9b761a0`).
`core.rls_permissive()` was `SELECT TRUE`, so with no tenant GUC `evercoat_app`
read **119 organizations and 137 projects** — the whole database. Now 0.
FORCE RLS deliberately **not** enabled, so `evercoat_owner` stays exempt by
ownership and migrations, the seeder and sign-in survive; the FORCE tripwires
in `test_024` and `test_011` are narrowed, not defused. Running the suite then
found what reading could not: **35 auth routes 403'd** because `get_principal`
resolves every caller through an unscoped read (→ 033, a SECURITY DEFINER
`core.principal_for_subject`), and the **system audit chain became unreadable
by its own writer** — which broke the *write*, because `INSERT ... RETURNING`
is a read (→ 034, NULL-safe equality). Suite **516/0/11**; proved by
falsification.

▶ **Shipped 2026-08-22 toward E5:** `packages/design-tokens` — one token
source, **31 computed** WCAG checks wired into CI's web job, **proved by
falsification** three ways. The accent is blue on purpose: green/amber/red are
reserved for derived disposition, so no chrome may use a status hue.

---

> ⚠️ **I58 scope note (added 2026-08-22):** the FORCE ROW LEVEL SECURITY cutover must include **`knowledge.documents` and `knowledge.chunks`**. Codex flagged their absence on the new tables; `relforcerowsecurity` is FALSE schema-wide by design and two tripwire tests assert it stays FALSE until the cutover, so forcing it piecemeal would break `core.memberships_for_subject` and with it sign-in. The app connects as `evercoat_app`, which owns nothing and holds no BYPASSRLS, so the retrieval path IS policy-bound today — what is missing is the owner-path defence in depth, and it is missing schema-wide.
>
> ⚠️ **I58 scope note (added 2026-08-23):** the cutover must also include **`core.users`** (migration 044) and the **fourth SECURITY DEFINER function**, `core.user_id_for_subject`. 044 enables RLS and deliberately leaves FORCE off for exactly 032's reason: `core.memberships_for_subject` and `core.principal_for_subject` are owner-owned definers that read `core.users` with no GUC, before an organization is chosen, and under FORCE they return zero rows and sign-in dies. `test_044_user_directory_is_not_global.py` asserts FORCE stays FALSE and names I58 in the failure message, so it is a tripwire rather than an obstacle. I58's step "re-owner each definer to a dedicated NOLOGIN role" now covers four functions, not three.

### 🟡 P3 — worth doing, not blocking

| # | Issue | Detail |
|---|---|---|
| **I10** | Realm JSON has mojibake (`�`) where em-dashes and `§` were. | Cosmetic in the file; the `_comment` keys carrying it are gone, but check `displayName` and any description strings. |
| **I11** | `promote_message` cannot target a decision/experiment/failure. | It creates a task only. §7 lists six promotion targets. Task first was deliberate; the rest is real scope. |
| **I12** | No `/api/messaging` UI. | The routes exist; `DiscussionPanel` is in the §12 reuse list and is unbuilt. |
| 🔴 **I13** | **Deploy of API + Keycloak — the only thing between this repository and a working product. MEASURED 2026-08-20, and now RE-TARGETED (see ADR-026).** | **What was measured.** `.github/workflows/render-provision.yml` (POST/GET only, no DELETE, name-scoped to `evercoat-itw-rd-*`, refuses rather than overwrites) was run against the real Render API with the repository's `RENDER_API_KEY`. Render refused both halves, verbatim: `POST /postgres` → **400 `"cannot have more than one active free tier database"`** (`autoworkshop-postgres` holds that slot and **expires 2026-09-01**), and `POST /services` plan=free → **400 `"free tier usage quota has been exhausted, new services are not allowed"`**. 🔴 **The key WORKS — those are 400s, not 401s.** `GET /owners` returned 200 (`tea-d86fu8mk1jcs7397i70g`). A new or rotated key produces the identical errors: this is a plan/billing boundary, not auth and not technical. **Do not spend a session rotating credentials.** 🔴 **ADR-026 (Railway) IS REVERSED — 2026-08-21, verified against Railway's own pricing. RAILWAY HAS NO FREE TIER.** It offers **$5 of one-time credit for 30 days**, then a Free plan with **$1 of credit per month**, and anything beyond a single lightweight service with no database needs **Hobby at $5/month minimum**. This app needs three tiers and a database, so the trial expires inside a month and then bills. The migration was never zero-cost compliant. It is also blocked on an interactive `railway login`, which is an owner action — and the operator's standing rule (2026-08-21) is that no task may be assigned to them. **Do not restart the Railway path.** Full compliance matrix in `Desktop\Evercoat-Hosting-Options-2026-08-21.pdf`. The best zero-cost alternative measured is **Coolify on Oracle Cloud Always Free** (open source, permanent, 4 ARM cores / 24 GB), which needs exactly one signup and is therefore also blocked by the same rule. ▶ *Superseded decision was:* replace Render with Railway's free tier — ADR-026. Every technical prerequisite already exists (`apps/api/Dockerfile`, `services/keycloak/evercoat-realm.json`, migrations, seed, CI-proven suite); what changes is the provider, not the artefact. ⚠️ **Not started, and it is blocked on one owner action:** the Railway CLI on this host is **unauthenticated** (`railway whoami` → `Unauthorized`, CLI v4.66.0 at `C:/Users/USER/nodejs/railway`), and there is **no `RAILWAY_TOKEN` repository secret** — `RENDER_API_KEY` is still the only one. |
| **I24** | **`projects.project_members` READ visibility is still organization-wide.** | Who is on a *restricted* project is readable by anyone in the organization. Held out of migration 027 deliberately — 027 closed the WRITE escalation, which was the one that opened every other table. This is a disclosure of membership, not of content. |
| 🔴 **I25** | **`TODO.md` — this file — was destroyed by its own last two updates, and recovered from git history.** | At tip `945b2fe` the file was **15,794,988 bytes across 63 lines**, with single lines up to **1.8 M characters**: the I13 table row concatenated into itself ~1,369 times. **Every issue except I13 had been deleted** — I3–I12, I14–I21 and I23 were all gone. Damage began at `24c5917` (11,940 bytes, I10–I13/I20/I21 already lost) and completed at `4598fc8` (15.8 MB). Recovered here from **`84733a9:TODO.md`** (17,815 bytes, register intact) with the I13 and I20 updates re-applied by hand. ⚠️ **Root cause not yet established** — it is almost certainly an append/rewrite step in the docs-update path that re-emits the whole file into one row. Find it before the next docs commit, or this recurs silently. It is not periodic repetition, so it cannot be un-done mechanically; only git history recovers it. |
| ~~I26~~ | ~~A conversation had no defined order.~~ | ✅ **CLOSED 2026-08-21** (migration 028, `50a07ab`). `messaging.messages.posted_at` defaulted to `now()`, which is **transaction-start time** — `SELECT now() = now()` is TRUE inside a transaction — so every message written in one transaction got an IDENTICAL timestamp and `ORDER BY m.posted_at` had nothing left to order by. PostgreSQL returned them in any order it liked, and a reply could render above the message it answered. Found because `test_023_messaging` failed against a local PostgreSQL while passing in CI: **CI had been green on heap luck.** Fixed in both halves — DEFAULT is now `clock_timestamp()` (makes the order right) and `ORDER BY m.posted_at, m.id` (makes it repeatable). Neither alone suffices. |
| **I27** | **`notifications.created_at` and `channels.created_at` have the same defect as I26 and were deliberately NOT changed.** | Both default to `now()` and are ordered DESC with no tiebreaker, so rows created in one transaction have no defined order. Left alone on purpose: both are newest-first display orders, neither has a test or a stated user expectation of intra-transaction ordering, and editing them on the strength of a nearby bug would be an unmeasured change. Do it deliberately, with a test that fails first. |
| **I28** | **028 gives a STABLE order, not a guaranteed INSERTION order.** | Raised by Codex against my own migration header, which claimed the stronger thing. `clock_timestamp()` + a random-UUID tiebreaker equals insertion order only at clock resolution: a wall clock can step backwards, equal timestamps break arbitrarily, and rows written before 028 carry the old shared `now()` value and their true order is **unrecoverable**. A real guarantee needs a monotonic key — a sequence column or per-channel counter — plus a backfill. |
| **I29** | **No test covers the request-boundary commit.** | `tests/db/*` fixtures roll back by contract so the suite leaves no residue on a developer's database, so every persistence assertion reads flushed-but-uncommitted rows through the same session. That proves the write reached PostgreSQL; it does not prove `session_scope` committed it. Raised by Codex and accepted as a known limit rather than closed. Belongs to an API-level test that drives a route and verifies from a separate connection. |
| ~~I30~~ | ~~22 `session.rollback()` calls inside IntegrityError handlers.~~ | ✅ **CLOSED 2026-08-21** (`ce56532`). `Session.rollback()` rolls back the **TOPMOST** transaction and discards nested ones — a savepoint around the call does not save you, you must REMOVE it. All converted to `guarded_write(session)` (`app/core/db.py`), one SAVEPOINT helper. 🔴 **The register said 22; the real number was 23** — the AST guard found two sites in `projects/planning.py` with NO `session.rollback()` at all, so a grep could never have listed them, and they still ran their handler over a transaction PostgreSQL had already aborted. Two permanent guards in `tests/test_no_transaction_destroyers.py`, both **proved by falsification**. ⚠️ The first version of the containment guard COULD NOT FAIL — it asked only whether a `guarded_write` appeared somewhere in the block, so an unguarded sibling write satisfied it. Caught by Codex. |
| ~~I31~~ | ~~`list_messages` returned the OLDEST page, so a busy channel could never show recent messages.~~ | ✅ **CLOSED 2026-08-21** (`c358625`). `ORDER BY posted_at ASC … LIMIT 100` returns the FIRST hundred messages ever posted, and there is no offset or cursor parameter — so past 100 messages every new post was unreachable and a reader was pinned to the opening of the conversation permanently. Now takes the newest page (`DESC … LIMIT`) and reverses it in Python for reading order; migration 029 adds `(channel_id, posted_at DESC, id DESC)` to serve it. Raised by the Supervisor. |
| ~~I32~~ | ~~The approval engine was EMPTY for every organization created after migration 020.~~ | ✅ **CLOSED 2026-08-21** (migration 030, `3f791d7`). 020 seeded §9's five templates with a **one-time** `FOR org IN SELECT id FROM core.organizations LOOP` — no trigger, and nothing else writes the table. So **no approval could be routed at all for a new tenant**: `open_route` finds no template for the authority level and refuses. Measured before the fix (a fresh org returned **0** templates) and after (**5 templates, 15 steps**). Now a `provision_approval_templates()` function + AFTER INSERT trigger + backfill, and the migration **verifies its own backfill**. ⚠️ Auto-provisioning surfaced that `approval_templates` held `ON DELETE RESTRICT` on the org, making an organization permanently undeletable — it broke **57 fixture teardowns**. Changed to CASCADE on the merits: these are derived CONFIGURATION, while `approval_routes` (the real decisions) keep RESTRICT. **Configuration cascades; history restricts.** |
| ~~I33~~ | ~~`notify()` had exactly ONE caller, so §11's actionable badge read zero.~~ | ✅ **CLOSED 2026-08-21** (`d67268a`). Only `_resolve_mentions` called it, so a chemist could be assigned every task in a project and see nothing. Two producers wired through the shared `notify()`: **task assigned → the assignee** (not the creator; a role assignment notifies nobody, since `my_work` already surfaces it and fan-out would make one task look like several), and **failure investigation opened → the PROJECT LEAD**. The second matters most: §10 opens investigations AUTOMATICALLY, so the most consequential record in the product was being created owned by nobody. The lead specifically because §7 forbids a notification disclosing what the recipient cannot see, and `lead_user_id` is what migration 006 already uses to grant read on a restricted project. |
| ~~I34~~ | ~~A property test claimed a guarantee the arithmetic cannot provide.~~ | ✅ **CLOSED 2026-08-21** (`d67268a`). Hypothesis broke `test_normalisation_preserves_component_ratios` with a ratio of 79.24 against a fixed **absolute** bound of 0.01. The defect was in the TEST: percentages quantise to `0.0001`, so for a ratio a/b the relative error is bounded by `(q/2)/a + (q/2)/b` — **the absolute error on a ratio grows with the ratio**. The bound is now DERIVED from the normalised values, and is **stricter, not looser**: proved by falsification, it catches a +0.0002 re-proportioning that the old 0.01 bound passed without noticing. |
| 🔴 **I35** | **My local quality gate was NARROWER than CI's and certified a red commit green.** | ✅ **CLOSED 2026-08-21** (`18de30e`) — recorded because the failure mode matters more than the fix. CI runs `ruff check . && ruff format --check .` from `apps/api`, which includes `migrations_alembic/` and `scripts/`. `scripts/handover.sh` printed `ruff check app/ tests/`, so **every migration and script sat outside the local gate**, and four commit messages reported it clean. `CLAUDE.md:292` had the correct command all along — two copies of a command in two files, disagreeing, which is this repository's most repeated defect applied to its own tooling. |
| **I36** | **`promote_message` writes `workflow.tasks` DIRECTLY instead of calling `create_task`.** | Found while wiring I33. `messaging/service.py` has its own INSERT, so it bypasses `create_task`'s assignee-membership check (which Codex added as C1 because `assigned_user_id` is a plain FK that accepts a user from another tenant) **and** it now bypasses the new task-assigned notification. Same shape as I5: a second implementation of something §12 names as shared. |
| ~~I37~~ | ~~`controlled` authority had no approval template, so those tests could not be reviewed at all.~~ | ✅ **CLOSED 2026-08-21** (migration 031). `testing.tests.authority_level` permits SIX values; migration 020 seeded FIVE templates. Harmless until I5 routed approval through the engine — then review raised, rolled back, and the test was stuck at `awaiting_review` **permanently**. CONTROLLED_OVERSIGHT is not invented: the deleted code mapped `controlled` → `test.approve_lead`, so it has always meant *a lead signs it*. The migration now **verifies every authority level has a ladder** and refuses to complete otherwise. |
| ~~I38~~ | ~~`request_additional_test` could stall a mandatory rung with no recorded reason.~~ | ✅ **CLOSED 2026-08-21** (migration 031). It was the one non-advancing decision `approval_route_steps_refusals_state_why` did not require a rationale for — a permanent, unexplained dead end on a rung no second decision can clear. §9 makes every decision a permanent record; one omitting the field that makes it actionable is not one. |
| **I39** | **An escalation cannot complete its own route.** | `escalate` advances REACHABILITY (so OVERSIGHT_STANDARD's escalation rung opens — the rung that exists *so an escalation has somewhere to land*) but deliberately NOT settlement: if it settled, a route would reach `approved` on an escalation alone, with **no approving signature anywhere and the test GREEN**. So the lead's view is recorded and the route stays open, and resolving an escalation goes the same way as a correction — re-review supersedes the stalled ladder. Clunky, and deliberately **not** "fixed" by inventing auto-approval semantics for a regulated approval chain. Proper fix needs the engine to model an escalation TARGET (which rung stands in for the escalated one). |
| ~~I40~~ | ~~`app/api/dashboards.py` has NO test at all.~~ | ✅ **CLOSED 2026-08-21** (`34cce9c`). Six route tests in `tests/auth/` (which mints its own tokens and needs no Keycloak). 🔴 **A laboratory_technician calling `/api/dashboards/director` now gets neither the innovation pipeline nor the decision queue** — and the seeded opportunity's title appears NOWHERE in the response body, because the two named panels could be right while a third leaked it. Plus: a forbidden panel says so over HTTP; the deviations panel is AVAILABLE for a technician (**the assertion that catches a phantom permission**, since no test can hand itself what no role holds); all four roles reachable; the 404 does not echo the path back; an anonymous caller is refused. **Proved by falsification** — removing the `opportunity.view` gate reproduces the leak. ⚠️ **Lesson: a service-level test cannot see a permission FLOOR that is too low, nor a gate naming a permission nobody holds.** Both HIGH findings passed the gate for that reason. |

---

## 2. Schedule to complete MVP-1

**Basis:** the owner's own budget — `ITWRD App.txt` L22,665 — 3 sessions/day
× 5 hours = **15 dev hours/day**, MVP-1 = 3 days = **9 sessions = 45 hours**.

Slices 1–7 backend is built. What remains is almost entirely the **browser
half**, which is what the acceptance gate actually measures.

| # | Session | Work | Hours | Exit condition |
|---|---|---|---|---|
| ~~S1~~ | ~~Auth, end to end~~ | ✅ **DONE**, except its exit condition, which needs a deployed Keycloak (I13 — now Railway, ADR-026) and a working deploy (B4). Built: PKCE flow, callback, provider, org switcher, `GET /api/me`, migration 024. **NOT** next-auth — ADR-025. | 5 | ⚠️ Exit condition NOT met: a human cannot sign in on the deployed shell, because no Keycloak is deployed. Everything below that line is proven in CI. |
| **S2** | Wire the read screens | Projects, Requirements, Materials, Formulations, Batches, Tests — swap `demo-data.json` for TanStack Query against the real routes. Keep the demo banner only where no route exists yet. | 5 | Six screens render database rows. `demo-data.json` referenced by ≤ 6 files. |
| **S3** | Wire the write paths | Create project → create formula → submit → approve lab. Forms with React Hook Form + Zod against the existing routes. | 5 | The first four golden-scenario arrows are drivable by hand in a browser. |
| **S4** | Lab + Test entry | Batch creation, sample, test creation, **raw per-replicate entry**. Derived status displayed as two separate fields (automatic evaluation *beside* final disposition). | 5 | A RED result can be produced through the UI. |
| **S5** | Approvals + failure UI | `ApprovalTimeline`, the 7 decision types, failure investigation screen, hypothesis states. **Fix I5, I6, I7 here** — the UI is what makes those holes visible. | 5 | YELLOW→GREEN happens by human approval, and a RED opens a failure. |
| **S6** | Dashboards (I4) | Four role dashboards, KPI cards, drill-down to real source records. **Fix I8** so counts are actionable items, not totals. | 5 | Every KPI drills to a real record. No panel can only ever show zero. |
| **S7** | Golden E2E (I3) | The 15-arrow scenario, asserted in UI **and** DB. Plus RBAC E2E and the MSD boundary suite. | 5 | Golden suite green in CI against a real Keycloak. |
| **S8** | Deploy + live suite | Deploy web (already static). API + Keycloak **on Railway per ADR-026, once `railway login` has been completed — and only on a confirmed free allowance; anything billable is an owner decision** — otherwise ship the browser-provable half and say so plainly. Run the full suite against the deployed site. | 5 | **passed / failed / skipped reported as three numbers** against the deployed URL. |
| **S9** | Governance + hardening | Codex 5-pass, Supervisor, `MEMORY.md` / `BRAIN.md` / `CHANGELOG.md` / `CONTEXT.md`, a11y sweep with axe on every new screen, `docs/REUSABILITY.md`. | 5 | Four gates pass. MVP-1 declared with evidence, not assertion. |

**Total: 45 hours = 9 sessions = 3 days at the owner's stated rate.**

### Two risks that would blow this schedule

1. **S8 is not fully in our control.** The API and Keycloak need an
   instance. Render's quota is exhausted and **ADR-026 re-targets those
   tiers at Railway's free offering** — but that is a *selected target*,
   not a migration: the Railway CLI here is unauthenticated and no
   `RAILWAY_TOKEN` secret exists, so it is blocked on an owner action.
   Until one of the two provides a free instance, the deployed artefact
   stays the static site and the *full* golden scenario is provable only
   in CI. **Say that plainly rather than reporting a green that means
   something narrower.**
2. **Docker on this host is wedged.** Everything is verified through CI,
   which is slower per iteration. Restarting Docker Desktop would fix it
   but restarts the `aw-*` stack — **which the operator has forbidden.**

---

## 3. Done this session — do not re-plan

- Messaging service + 6 routes (103 total). Schema 022 finally has a writer.
- Migration **023** — `audit.deny_mutation()` named the wrong table.
- **Keycloak runs, for the first time**, in CI: bootstrap script, subject
  binding, 6 auth integration tests, 14 realm invariant tests.
- **The shipped realm was unimportable since Slice 1** — seven `_comment`
  keys, four of them nested. Fixed; commentary moved to
  `services/keycloak/realm/README.md`.
- Mention-notification leak fixed (author's session vs recipient's access).
- All four Codex findings fixed.
- `scripts/assert-suite-ran.py` — three numbers, never an exit code.